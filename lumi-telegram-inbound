#!/usr/bin/env bash
# v0.13 Telegram bridge — daemon 1: Telegram → inbox.jsonl. Run alongside ./lumi (the TUI is the brain).
# Runs from the repo root regardless of where it's invoked from. See docs/TELEGRAM_SETUP.md.
cd "$(dirname "$0")" && exec uv run python -m telegram.inbound "$@"
