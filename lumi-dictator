#!/usr/bin/env bash
# v0.26 local dictation (STT): hears your speech and types it into the chat (toggle with Ctrl+D in the TUI).
# Run alongside ./lumi. Needs the `dictation` extra. See docs/DICTATION_SETUP.md.
cd "$(dirname "$0")" && exec uv run --extra dictation python -m voice.dictator "$@"
