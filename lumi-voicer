#!/usr/bin/env bash
# v0.14 local voicer (TTS): speaks each new reply aloud (reads the outbox bus). Run alongside ./lumi.
# Needs the `voice` extra. See README.md and docs/DICTATION_SETUP.md.
cd "$(dirname "$0")" && exec uv run --extra voice python -m voice.voicer "$@"
