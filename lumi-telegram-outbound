#!/usr/bin/env bash
# v0.13 Telegram bridge — daemon 2: outbox.jsonl → Telegram. Run alongside ./lumi (the TUI is the brain).
# Runs from the repo root regardless of where it's invoked from. See docs/TELEGRAM_SETUP.md.
cd "$(dirname "$0")" && exec uv run python -m telegram.outbound "$@"
