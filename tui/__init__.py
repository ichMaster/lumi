"""TUI — the terminal interface (Textual).

In-process over the core in v0; refactored into a server client in v1.6. A thin
client: it imports ``core`` and renders — no model/SDK or storage logic.
"""
