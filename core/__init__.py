"""Core — Лілі's interface-independent mind.

Canon, per-user memory (from v0.2), model invocation via the thin ``LLMClient``
seam, emotion-field assembly + validation (from v0.3), and the ``Repository``
interface. The core knows nothing about who renders it (TUI, CLI, web, server).
"""
