# Version v1.3 — Execution Report

**Date:** 2026-07-15
**Branch:** v1.1-dev
**Label:** v1::version:1
**Phase:** v1.3 — Explicit Gemini prompt cache (TTL 1h): warm prefill across pauses
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-184 | Probe — `cachedContents` on the active Gemini model (`--cache`) | v1.3 | completed | 74bfbb2 | 3 | pass (1447) |
| 2 | LUMI-185 | `latency_ms` on every cache-log record + report median column | v1.3 | completed | 2408d5d | 3 | pass (1450) |
| 3 | LUMI-186 | Explicit `cachedContents` lifecycle in `GeminiClient` | v1.3 | completed | fb5791e | 3 | pass (1457) |
| 4 | LUMI-187 | Observability, continuity report, docs, off-pin | v1.3 | completed | b644515 | 6 | pass (1460) |

## Detailed Results

### LUMI-184: Gemini explicit-cache probe

**Status:** completed · **Commit:** 74bfbb2 · **Files:** `scripts/gemini_probe.py`, `tests/unit/test_gemini_probe.py`, `v1.3-issues.md`

- `--cache` mode: create `cachedContents` (~12K-tok system, TTL 1h) → reference + `cachedContentTokenCount`
  hit-check → the **cache + own `systemInstruction`** constraint step (expected rejected) → TTL PATCH →
  DELETE. Pure builders; 7 unit tests drive the full lifecycle via a mock transport.
- **⚠️ The live verdict is a manual, paid, never-CI run** — recorded as PENDING in `v1.3-issues.md`.

### LUMI-185: `latency_ms` on every cache-log record

**Status:** completed · **Commit:** 2408d5d · **Files:** `core/cache_log.py`, `core/agent.py`, `tests/unit/test_cache_log.py`

- `CacheEvent.latency_ms` (None-defaulted → legacy loads); threaded from `stats.latency_ms` (already
  measured per call — one seam, all providers). By-channel report gains a **Median latency** column.

### LUMI-186: Explicit `cachedContents` lifecycle in `GeminiClient`

**Status:** completed · **Commit:** fb5791e · **Files:** `core/config.py`, `core/llm.py`, `tests/unit/test_gemini_client.py`

- One handle per (model, sha256(prefix)): create from `cache_prefix` (TTL from `LUMI_PROMPT_CACHE_TTL`),
  reference every reply (the volatile tail rides as leading `contents`, **no request `systemInstruction`**
  — the LUMI-184 constraint), recreate on prefix/model change + best-effort delete, degrade to implicit on
  ANY cache-API failure (logged once, the turn never fails). Wired into the single-call **and** tool-loop
  bodies. `LUMI_GEMINI_EXPLICIT_CACHE` (off). 8 tests via injected transports.

### LUMI-187: Observability, continuity report, docs, off-pin

**Status:** completed · **Commit:** b644515 · **Files:** `core/cache_log.py`, `core/agent.py`, `core/llm.py`, `tests/unit/test_cache_log.py`, `docs/PROMPT_OPTIMIZATION_III.md`, `specification/ROADMAP.md`

- `last_cache_event` sticky across tool-loop rounds → the Core logs it on the turn's first record
  (`cache_event` field). Report: an **Explicit cache (Gemini)** section + a **Continuity** table (post-gap
  reply turns with `cache_read` + `latency_ms` — the warm-across-pauses proof); absent when the feature
  never ran. Docs: the before/after method in PROMPT_OPTIMIZATION_III (pending an operator run), the flag
  in `.env`, the probe-verdict note on the ROADMAP phase.

## Validation (final)

- **pytest: 1460 passed** (21 new across the phase), **no paid calls** (mock transports for the probe,
  the Gemini caches API, and the model throughout).
- **ruff: clean.**
- **Off (`LUMI_GEMINI_EXPLICIT_CACHE`) → byte-identical** Gemini request payloads + no cache events
  (pinned); the `{reply, emotion, intensity}` contract and prompt text untouched. `latency_ms` /
  `cache_event` are additive (legacy records load unchanged).

## ⚠️ Manual step remaining (paid, operator)

Before relying on the explicit cache in production, run the LUMI-184 live probe and record the verdict:

```
GEMINI_API_KEY=…  uv run python scripts/gemini_probe.py --cache gemini-3.1-pro-preview
```

If the `cache+systemInstruction` step returns ACCEPTED (not REJECTED), the LUMI-186 "volatile tail in
the leading `contents`" design must be revisited. Then measure the before/after (PROMPT_OPTIMIZATION_III
table) from two `.lumi/cache-report.md` snapshots (flag off vs on, post-20-min-pause reply).

## Next Steps

The v1.3 phase is complete — all 4 issues closed. Release (`/release-version 1.3.0`) awaits explicit
confirmation. The next roadmap phase is **v1.4 — Topic base** (the conversation-development system
resumes). Note: `main` is still at v1.0.0; v1.1.0 / v1.2.0 / (pending) v1.3.0 live on `v1.1-dev`.
