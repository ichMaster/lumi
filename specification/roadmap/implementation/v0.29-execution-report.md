# Version v0.29 — Execution Report

**Date:** 2026-06-18
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.29 — Local file tool III: metadata + create-folder + copy (non-destructive)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All three issues (LUMI-113 → LUMI-114 → LUMI-115) implemented, validated, committed, pushed, and closed
in dependency order. The full suite grew from **924 → 942** tests (+18), all green.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-113 | core/files.py — dates + stat_file + create_folder + copy_file (non-destructive) | v0.29 | completed | b1eedcd | 3 | pass |
| 2 | LUMI-114 | Register the new file tools behind LUMI_FILE_TOOL + config + .env.example + docs | v0.29 | completed | 46f0eb0 | 3 | pass |
| 3 | LUMI-115 | Contract + per-user isolation + behaviour tests (file tool III) | v0.29 | completed | 77297fd | 1 | pass |

## Detailed Results

### LUMI-113: core/files.py — dates + stat_file + create_folder + copy_file

**Status:** completed
**Commit:** b1eedcd
**Files changed:**
- `core/files.py` (modified) — dates on `list_files`; new `stat_file` read tool; `create_folder` +
  `copy_file` write tools (create-only); `_stat_dates`/`_fmt_ts` helpers; `copy_max` ctor arg; the new
  schemas added to `READ_TOOLS` / `WRITE_TOOLS` and dispatched in `FileTools.execute`.
- `core/config.py` (modified) — `file_copy_max` (`LUMI_FILE_COPY_MAX`, default 5 MB) field + loader.
- `tests/unit/test_files.py` (modified) — updated tool-shape tests; v0.29 unit section (dates/stat,
  `st_birthtime`→`st_ctime` fallback, create-only folder, copy create-only + size cap + traversal).

**Validation:**
- [x] Unit tests (test_files.py): pass (37)
- [x] Full suite: pass (935)
- [x] Lint (ruff): pass · py_compile: pass
- [x] Acceptance criteria: all pass

---

### LUMI-114: Register the new file tools + config + .env.example + docs

**Status:** completed
**Commit:** 46f0eb0
**Files changed:**
- `core/agent.py` (modified) — `file_copy_max` threaded `Core.__init__` → assignment →
  `FileTools(copy_max=...)` in `_file_tool_args` → `build_core` (`file_copy_max=cfg.file_copy_max`). The
  v0.29 tools auto-register via `READ_TOOLS` + `WRITE_TOOLS` behind the existing `LUMI_FILE_TOOL` flag.
- `.env.example` (modified) — `LUMI_FILE_COPY_MAX` entry + the file-tool block header updated for v0.29.
- `docs/FILE_TOOL_SETUP.md` (modified) — tool table (+ `stat_file` / `create_folder` / `copy_file`),
  config table, and env example updated. (FILE_TOOL.md already described the v0.29 surface — verified.)

**Validation:**
- [x] Full suite: pass (935)
- [x] Lint (ruff): pass · py_compile: pass
- [x] Acceptance criteria: all pass (no new flag — reuses `LUMI_FILE_TOOL`; no emotion-contract change)

---

### LUMI-115: Contract + per-user isolation + behaviour tests

**Status:** completed
**Commit:** 77297fd
**Files changed:**
- `tests/contract/test_file_tool_contract.py` (modified) — `_core` helper gained `file_copy_max`; a v0.29
  contract section (7 tests): off→absent / on→present (`_turn_tools`), per-user isolation over
  `stat_file` + `copy_file`, copy create-only dest + source-size cap + traversal on both paths,
  `create_folder` create-only, `stat_file` size+dates, and the `{reply, emotion, intensity}` contract
  with the v0.29 tools active.

**Validation:**
- [x] File contract tests: pass (18, +7)
- [x] Full suite: pass (942)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

## Notes

- **No contract change.** `set_state` stays terminal; the `{reply, emotion, intensity}` schema and its
  contract test are untouched. The v0.29 tools are additive on the shipped v0.19/v0.20 executor.
- **Non-destructive guarantee held:** create-only folder + copy-dest, no overwrite / delete / move path
  anywhere; per-user isolation pinned over the new tools.
- **Off by default:** the v0.29 tools reuse the existing `LUMI_FILE_TOOL` flag — no new toggle.

## Next Steps

- The v0.29 phase is complete. The version has **not** been bumped — release via `/release-version 0.29.0`
  when desired.
- Next roadmap phase: **v0.30 — Semantic recall III: chunking long messages**.
