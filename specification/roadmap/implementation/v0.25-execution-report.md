# Version v0.25 — Execution Report

**Date:** 2026-06-17
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.25 — News tool (Guardian: search & read)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All three v0.25 issues implemented, validated (model + Guardian API mocked — **no network, no key, no
paid calls**), committed, pushed, and closed in dependency order. The full suite grew from 821 → **847
tests** (+26). The emotion-channel contract (`{reply, emotion, intensity}`, `set_state` terminal) and the
per-user isolation invariant are unchanged; the core stays SDK-free (the Guardian provider is an injected
seam).

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-101 | `core/news.py` — `NewsProvider` seam + `GuardianProvider` + `news_search`/`news_read` + per-turn id registry + config | v0.25 | completed | 2bdfb85 | 3 | pass |
| 2 | LUMI-102 | Register the news tools on `Core.reply` behind `LUMI_NEWS_TOOL` (`_turn_tools` merge, no-personal-data query, EN→UK canon line, caps) + docs | v0.25 | completed | 1e34128 | 5 | pass |
| 3 | LUMI-103 | Contract + untrusted (EN+UK) + no-personal-data + off-turn-id + caps + isolation + emotion-contract tests | v0.25 | completed | 578a522 | 1 | pass |

## Detailed Results

### LUMI-101: NewsProvider seam + GuardianProvider + the two tools + id registry

**Status:** completed · **Commit:** 2bdfb85
**Files changed:**
- `core/news.py` (new) — a `NewsProvider` Protocol + **`GuardianProvider`** over an injected `http_get`
  (single Guardian host; `read` fetches **by content-id**, never a raw URL); `NewsItem` (with `content_id`
  + the opaque `id`); **`NewsTools`** with `news_search` / `news_read` + a **per-turn opaque-id registry**
  (`n1`/`n2` → item). `execute` always returns a string (never raises). `NEWS_TOOLS` schema with the
  EN-query / UK-reply hint.
- `core/config.py` — the `LUMI_NEWS_*` namespace (tool / key / url / sections / max-results / max-chars /
  max-calls / days), **distinct** from the v0.4 ambient `LUMI_NEWS_URL`/`_CAP`.
- `tests/unit/test_news.py` — 12 unit tests (mock transport).

**Validation:** unit 12 pass · config load pass · ruff/compile pass.

---

### LUMI-102: wire into Core.reply + safety + canon + docs

**Status:** completed · **Commit:** 1e34128
**Files changed:**
- `core/agent.py` — `_news_tool_args` (a fresh `NewsTools` + `GuardianProvider` over the injected
  `http_get` per turn, behind `LUMI_NEWS_TOOL`) registered in `_turn_tools`; a per-turn
  `LUMI_NEWS_MAX_CALLS` cap; the query passes through unchanged (no personal data); the `NEWS_DIRECTIVE`
  added to the system prompt when on (EN query / UK reply, cited, honest); config threaded
  `build_core` → `Core` (+ injected `news_http_get`).
- `core/news.py` — the authored `NEWS_DIRECTIVE`.
- `.env.example` + `docs/NEWS_SETUP.md` — the `LUMI_NEWS_*` block + the operator guide (key signup, the
  two tools, EN→UK, safety, the v0.4-ambient distinction).
- `tests/integration/test_news_tool.py` — 4 tests (mock model + mock transport).

**Validation:** integration 4 pass · news/wiki/image/contract regression 110 pass · full suite 837 pass ·
ruff/compile pass. No contract change.

---

### LUMI-103: contract + safety tests

**Status:** completed · **Commit:** 578a522
**Files changed:**
- `tests/contract/test_news_contract.py` — 10 tests: untrusted bodies in **English AND Ukrainian**
  (emotion unchanged, framed as data through the real client loop); the outgoing query carries only the
  model's topic; an off-turn/unknown id is refused; the per-turn id registry is **per-user isolated**; the
  per-turn + body caps hold; off → tools absent; an HTTP error degrades; the emotion contract holds.

**Validation:** contract 10 pass · ruff (core/+tests/) pass · **full suite 847 pass** — no network, no
key, no paid calls.

## DoD check (ROADMAP §v0.25)

- [x] With the flag on (+ a key), a turn searches Guardian and reads an article, answering in Ukrainian
      **with the source**.
- [x] An injection inside a body (EN or UK) is ignored; no personal/memory data appears in the outgoing
      query; `news_read` refuses an id not from this turn; per-turn + size caps + logging hold.
- [x] Off (default) → the tools are absent; the `{reply, emotion, intensity}` contract test passes
      verbatim; the Guardian API is mocked (no paid calls).

## Next Steps

The v0.25 phase is complete (3/3 issues). The version has **not** been bumped — cut the release with
`/release-version 0.25.0` when ready. The next roadmap phase is **v0.26 — Local dictation (STT)**
(`specification/features/DICTATION_LOCAL.md`). A later rung could add a Ukrainian-local RSS source behind
the same `NewsProvider` seam without touching the two tools.
