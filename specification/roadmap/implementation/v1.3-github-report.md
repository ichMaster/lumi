# Version v1.3 — GitHub Issues Report

**Uploaded:** 2026-07-14
**Repository:** https://github.com/ichMaster/lumi
**Phase:** v1.3 — Explicit Gemini prompt cache (TTL 1h): warm prefill across pauses
**Total issues:** 4

## Issue Mapping

| LUMI ID | GitHub # | Title | Phase | Labels | URL |
|---------|----------|-------|-------|--------|-----|
| LUMI-184 | #184 | Probe — verify `cachedContents` on the active Gemini model (`scripts/gemini_probe.py`) | v1.3 | v1::version:1, v1::size:S, v1::area:scripts | https://github.com/ichMaster/lumi/issues/184 |
| LUMI-185 | #185 | `latency_ms` on every cache-log record (all providers) | v1.3 | v1::version:1, v1::size:S, v1::area:core | https://github.com/ichMaster/lumi/issues/185 |
| LUMI-186 | #186 | Explicit-cache lifecycle in `GeminiClient` (create/reference/recreate/fallback, TTL from config) | v1.3 | v1::version:1, v1::size:L, v1::area:core | https://github.com/ichMaster/lumi/issues/186 |
| LUMI-187 | #187 | Observability, continuity report, docs, off-pin | v1.3 | v1::version:1, v1::size:M, v1::area:core, v1::area:docs | https://github.com/ichMaster/lumi/issues/187 |

## Dependencies

- **#184** (probe) and **#185** (`latency_ms`) are independent — **run in parallel, start here**.
- **#186** (the lifecycle — the core of the phase) is **blocked by #184** — comment posted.
- **#187** (observability + report + docs + off-pin) is **blocked by #186 and #185** — comment posted.

Build order: **#184 ∥ #185 → #186 → #187**.

## Labels Created

- v1::area:scripts (new; dev scripts / probes)
- v1::area:docs (new; docs / specs)
- (existing) v1::version:1, v1::size:S, v1::size:M, v1::size:L, v1::area:core

## Notes

- **Speed phase, not cost:** the DoD metric is the **post-pause warm prefill** (`cache_read` ≈ the
  ~12 K-tok prefix after a > 5-min gap) and the `latency_ms` delta — not the token bill.
- **LUMI-184 is manual + paid (cents), never CI** — it de-risks the preview model
  (`gemini-3.1-pro-preview`) before any core code; its verdict (min cache size, the
  `systemInstruction`-beside-`cached_content` constraint) must be recorded in the issues file and
  #186 before #186 starts.
- **Scope guard:** the reply channel only; the think channel's 0%-cache problem is
  PROMPT_OPTIMIZATION_III **T5** (a prompt-shape fix first), explicitly not this phase.
- **Off (`LUMI_GEMINI_EXPLICIT_CACHE`) → byte-identical** requests (pinned per issue); the
  `{reply, emotion, intensity}` contract, per-user isolation, and the prompt text are untouched.
- Background docs: [PROMPT_OPTIMIZATION_III.md](../../../docs/PROMPT_OPTIMIZATION_III.md) (the
  cached-vs-uncached measurement that motivated the phase), [LATENCY.md](../../features/LATENCY.md)
  (where the explicit cache sits among the speed levers).
