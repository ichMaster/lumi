# Version v0 (phase v0.11) — Execution Report

**Date:** 2026-06-09
**Branch:** main
**Label:** v0::version:0
**Phase:** v0.11 — Face variants & mood themes
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-042 | Variant resolver (random, no-repeat) + extended signal parse | v0.11 | completed | 0561fc8 | 2 | pass |
| 2 | LUMI-043 | Theme manifest (`faces/themes.md`) + folder auto-discovery | v0.11 | completed | 72e8f17 | 2 | pass |
| 3 | LUMI-045 | Authoring — `viewer/faces/PROMPTS.md` for variants + wardrobes | v0.11 | completed | 73f5857 | 1 | n/a (doc) |
| 4 | LUMI-044 | Mood picks the theme + core writes the themed face signal | v0.11 | completed | 3a8b626 | 6 | pass |

Final suite: **365 passed**, ruff clean. No paid API calls (model mocked; tests run against a
**fake faces tree** — no real images). **No contract change** — the emotion channel, the v0.7
signal/fallback, and the v0.6 mood are reused; flat v0.7 packs behave unchanged.

## Detailed Results

### LUMI-042: Variant resolver (random, no-repeat) + extended signal parse

**Status:** completed · **Commit:** 0561fc8 · `viewer/face.py`, `tests/unit/test_viewer_face.py`

- `resolve_variants((theme, emotion), …)` → the folder's `*.png` set with a fallback chain
  (`<theme>/<emotion>/` → `<theme>/calm/` → `<default_theme>/…` → flat v0.7 single).
- `pick_variant` — random pick, **no immediate repeat**. `parse_signal` → `(theme, emotion,
  intensity)` (a leading non-emotion token before an emotion is the theme; bare → default).
- `FaceSwitcher` gains `lister`/`default_theme`/`rng`: re-picks a random variant each new turn,
  relaxes to the theme's `calm` on idle. Flat v0.7 packs unchanged.

### LUMI-043: Theme manifest (`faces/themes.md`) + folder auto-discovery

**Status:** completed · **Commit:** 72e8f17 · `viewer/themes.py`, `tests/unit/test_viewer_themes.py`

- `load_themes(faces_dir) → Themes{descriptions, default}` from the editable `faces/themes.md`
  (`## <name>` + one-line description, `default:` directive) merged with auto-discovered theme
  folders (a `faces/` subdir with a `calm/`). Missing manifest → flat v0.7; unknown default →
  first discovered.

### LUMI-045: Authoring — `viewer/faces/PROMPTS.md`

**Status:** completed · **Commit:** 73f5857 · `viewer/faces/PROMPTS.md`

- Added a v0.11 section: the `faces/<theme>/<emotion>/NN.png` layout (required per-theme `calm/`),
  the variant recipe, the theme/wardrobe recipe (constant FACE block + per-theme WARDROBE &
  ATMOSPHERE block), the `themes.md` manifest format, and a pointer to MOODBOARD.md (the ten moods).
  Authoring doc only — no runtime impact.

### LUMI-044: Mood picks the theme + core writes the themed face signal

**Status:** completed · **Commit:** 3a8b626 · `core/mood.py`, `core/agent.py`, `core/config.py`,
`viewer/app.py`, `specification/ARCHITECTURE.md`, `tests/unit/test_face_themes.py`

- `mood_request(…, themes=)` appends the option list + asks for a trailing «ТЕМА: <name>» line;
  `split_theme`/`strip_theme` parse it out (kept off the resolution). `MoodState.theme` validated
  against the manifest (unknown → default), cached per local day.
- `Core.theme` property; `_write_face_signal` emits `<theme> <emotion> <intensity>` (bare when no
  theme). `build_core` loads `faces/themes.md` (`LUMI_FACES_DIR`) via a local import — the Core
  class stays viewer-independent. The viewer passes the default theme to `FaceSwitcher`.

## Phase DoD

Met ([ROADMAP.md §v0.11](../../ROADMAP.md)): the viewer shows a **different picture among several**
for the same emotion (**no immediate repeat**); the **outfit/theme changes with the mood of the
day** (stable within a day via the per-day cache, re-picked at local midnight); with **no
themes/variants present it behaves exactly like v0.7**. **No contract change** — the emotion
channel, the v0.7 signal/fallback, and the v0.6 mood are reused. All tests against the **mock
model + a fake faces tree** (no paid calls, no real images).

## Next Steps

- All v0.11 issues complete. **Version not bumped** (awaiting confirmation) — cut with
  `/release-version 0.11.0` when ready.
- **Authoring follow-up (no code):** generate the actual theme image packs
  (`viewer/faces/<theme>/<emotion>/NN.png`) + a `faces/themes.md` from
  [viewer/faces/MOODBOARD.md](../../../viewer/faces/MOODBOARD.md) (the ten "Honest Moods" + a neutral
  default). Until then the code path is fully graceful (flat v0.7).
- Next phase: **v0.12 — Inner life I: plans & state**.
