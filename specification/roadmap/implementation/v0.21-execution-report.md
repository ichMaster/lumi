# Version v0.21 — Execution Report

**Date:** 2026-06-16
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.21 — Wikipedia tool (custom tool: search & read)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All three v0.21 issues implemented, validated (model + HTTP mocked — no network, no paid calls),
committed, pushed, and closed in dependency order. The full suite grew from 719 → **743 tests** (+24).
The emotion-channel contract and per-user isolation invariant are unchanged.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-088 | core/wiki.py — wiki_search + wiki_read over an injected HTTP client + extract/size caps + config | v0.21 | completed | 1cb9f1e | 3 | pass |
| 2 | LUMI-089 | Register the wiki tools on the Core.reply tool-loop behind LUMI_WIKI (merge with file tools, safety, docs) | v0.21 | completed | 6234db3 | 4 | pass |
| 3 | LUMI-090 | Contract + untrusted-content + no-personal-data + caps + emotion-contract tests | v0.21 | completed | 9293802 | 1 | pass |

## Detailed Results

### LUMI-088: core/wiki.py — wiki_search + wiki_read over an injected HTTP client

**Status:** completed · **Commit:** 1cb9f1e
**Files changed:**
- `core/wiki.py` — `WikiTools` executor + `WIKI_TOOLS`/`WIKI_TOOL_NAMES`: `wiki_search` (opensearch →
  candidates) + `wiki_read` (REST `page/summary` → HTML-free extract + source), over an injected
  `http_get`; language fallback (`uk,en`), size cap, never-raises. Tool names Anthropic-safe (no dots).
- `core/config.py` — `LUMI_WIKI` (off) + `LUMI_WIKI_LANG` / `_BASE_URL` / `_MAX_CHARS` / `_MAX_CALLS`.
- `tests/unit/test_wiki.py` — 12 unit tests against a mock transport.

**Validation:** unit 12 pass · ruff pass · py_compile pass · full suite 731 pass. Acceptance: all pass.

---

### LUMI-089: wire the wiki tools into Core.reply + safety + docs

**Status:** completed · **Commit:** 6234db3
**Files changed:**
- `core/agent.py` — new `_turn_tools()` merges the v0.19/v0.20 file tools + the v0.21 wiki tools into
  one tool list + a **name-routing** executor (trace applied once); `_file_tool_args` now raw,
  `_wiki_tool_args` new (injected `http_get`, per-turn `LUMI_WIKI_MAX_CALLS` cap, query passed through
  unchanged). `Core.reply` calls `_turn_tools`; config threaded `build_core` → `Core`.
- `.env.example` — the `LUMI_WIKI*` block (untrusted / no-personal-data / off-by-default).
- `docs/WIKI_SETUP.md` — operator guide.
- `tests/integration/test_wiki_tool.py` — search→read→answer, off→no tools, query unchanged,
  file+wiki coexist, per-turn cap.

**Validation:** integration pass + file/trace/contract regression green (26/26) · ruff pass ·
full suite 736 pass. No contract change. Acceptance: all pass.

---

### LUMI-090: contract + safety tests

**Status:** completed · **Commit:** 9293802
**Files changed:**
- `tests/contract/test_wiki_contract.py` — 7 tests: untrusted extract reaches the model framed as
  data + emotion unchanged (real AnthropicClient loop); the query is exactly the model's request (no
  memory/personal data leak); extract size cap + per-turn call cap through a full turn; tools absent
  when off; HTTP error degrades; the `{reply, emotion, intensity}` contract holds.

**Validation:** contract 7 pass · ruff (core/ + tests/) pass · full suite **743 pass** — no network,
no paid calls. Acceptance: all pass.

## DoD check (ROADMAP §v0.21)

- [x] With the flag on, a turn can search Wikipedia and read an article's summary, answering with the
      **source**.
- [x] An injection attempt inside the extract is ignored (untrusted data).
- [x] No personal/memory data appears in the outgoing query.
- [x] Per-turn call cap + the extract size cap + logging hold.
- [x] Off (default) → the tools are absent.
- [x] The emotion-channel contract test passes verbatim.

## Next Steps

The v0.21 phase is complete (3/3 issues). The version has **not** been bumped — cut the release with
`/release-version 0.21.0` when ready (post-release fixes bump `0.21.x`). The next roadmap phase is
**v0.22 — Local dictation (STT)** (moved here from v0.21 in the recent reorder;
`specification/features/DICTATION_LOCAL.md`).
