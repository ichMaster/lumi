# Version v1.6.1 — Execution Report

**Date:** 2026-07-28
**Branch:** v1.1-dev
**Label:** v1::version:1
**Phase:** v1.6.1 — The live voice prototype (the first v1.6 slice)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-196 | Core.prompt_snapshot() — the assembled system prompt without a turn | v1.6.1 | completed | f60200a | 2 | pass |
| 2 | LUMI-197 | scripts/realtime_probe.py — mic ↔ Realtime ↔ speaker, pure context | v1.6.1 | completed | 4677b93 | 4 | pass |
| 3 | LUMI-198 | The live run — findings back into the spec + docs | v1.6.1 | completed | 213206a | 6 | pass |

## Detailed Results

### LUMI-196: Core.prompt_snapshot() (#196)

**Status:** completed · **Commit:** f60200a

A public `Core.prompt_snapshot(session)` — the SAME system-prompt builder a reply turn uses
(minus the per-message RAG blocks), with the same lazy ensures, no model call, nothing persisted.
The probe's "pure context" rides this snapshot; a contract test pins `snapshot == last_prompt["system"]`
for a recall-off turn.

**Validation:** 4 unit tests (canon+memory present, no model call / no persistence, stable, matches
the reply turn's system) · ruff pass.

### LUMI-197: scripts/realtime_probe.py (#197)

**Status:** completed · **Commit:** 4677b93 (+ live-run hotfixes through 7cece0c, 213206a)

The standalone **manual + paid** probe (never CI): mic ↔ OpenAI Realtime WS ↔ speaker, session
`instructions` = SPEECH_STYLE (accent steering + the no-tags rule) + the LUMI-196 snapshot.
Pure, unit-tested helpers (`build_session_update`, `ProbeDispatcher` with injected clock,
`parse_cli`, pcm framing, `strip_service_tags`); `run()` is uncovered glue. `./scripts/voice.sh
[voice] [--model …]` launcher (refuses while the TUI runs); a `realtime` extra
(`websockets`, `sounddevice`). Live-run hardening: lossless speaker buffer (the truncating
callback garbled the voice), startup beep, `--devices`/`--out`, mic watchdog, live API-error
prints, per-run voice pick.

**Validation:** 12 unit tests (payload shape, CLI, latency math with a fake clock, transcripts,
legacy aliases, barge-in, summary, tag stripping, known-quiet events) · ruff pass · no network,
no audio hardware, no paid calls in tests.

### LUMI-198: The live run — findings back into the spec + docs (#198)

**Status:** completed · **Commit:** 213206a

Three flagship (`gpt-realtime-2.1`) sessions run by the owner on 2026-07-28, recorded into
VOICE_MODE.md §7 ("Probe result") + a ✅ verdict on the ROADMAP v1.6.1 DoD:

- **Latency — GO ✅:** speech-stop → first audio **median 1.25–1.37 s** (min 0.41 / max 2.8) for
  the FULL loop (ASR → generation → audio out); the inherited ≤2.5 s target beaten on the median.
- **Voice — NO-GO ❌:** a strong American accent in Ukrainian, unmoved by OpenAI-recommended
  steering across voices (marin/shimmer) even on the flagship; noticeably better after the
  no-tags fix, still clearly below her ElevenLabs voice → the **ElevenLabs hybrid** (realtime
  brain + `output_modalities:["text"]` + streaming TTS) is the primary voice-identity candidate;
  OpenAI custom voices are gated to eligible customers.
- **Persona — PARTIAL ⚠:** the snapshot carries memory/canon (the owner's name, Lili Jinx,
  Пічка/hill-303, a stable mood), but the TEXT-protocol tags leaked into **speech**
  (`<emotion>calm 0.7</emotion>` spoken aloud — English tokens feeding the accent). Fixed: a
  leading no-tags rule in SPEECH_STYLE + `strip_service_tags()`; v1.6.3 needs a voice-mode
  prompt variant.
- **Event shapes discovered:** `conversation.item.input_audio_transcription.delta`,
  `response.output_audio.done` — registered as known-quiet in the dispatcher.
- `.env.example` gets the realtime block (`LUMI_REALTIME_MODEL`/`LUMI_REALTIME_VOICE`).

**Validation:** 1614 tests pass · ruff pass.

## Next Steps

- v1.6.1 is complete; a `1.6.1` release can be cut via `/release-version` on the owner's call.
- v1.6.2 (mode framework + transport MVP, Core stays the brain) is the next slice; the discovered
  event shapes and the voice/persona verdicts above are its inputs.
- Open follow-ups for v1.6.3: the ElevenLabs-hybrid decision as the official voice path; a
  voice-mode prompt variant (text-protocol sections stripped/overridden).
