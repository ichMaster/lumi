# Version v0.19 — Execution Report

**Date:** 2026-06-15
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.19 — Local file tool I: reading (tool-loop + see / search / read)
**Target version:** 0.19.0 (not yet released — awaiting confirmation)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 5 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All five issues implemented, validated (ruff + the full pytest suite, **682 passed, model mocked, no
paid API calls**), committed one-per-issue, pushed, and closed on GitHub.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Tests |
|---|---------|-------|-------|--------|--------|-------|
| 1 | LUMI-080 | core/files.py — sandboxed read executor + 3 read tools | v0.19 | completed | 41d8f9a | pass |
| 2 | LUMI-081 | Bounded tool-loop in the LLMClient seam | v0.19 | completed | 9a67183 | pass |
| 3 | LUMI-082 | Wire the loop + file executor into Core.reply + config | v0.19 | completed | 9ebebf6 | pass |
| 4 | LUMI-083 | Safety bounds & graceful degradation | v0.19 | completed | 65cd9d5 | pass |
| 5 | LUMI-084 | Contract + isolation + untrusted-content tests | v0.19 | completed | b85ebb9 | pass |

## Detailed Results

### LUMI-080: core/files.py — sandboxed read executor + 3 read tools

**Status:** completed · **Commit:** 41d8f9a
**Files:** `core/files.py` (added), `core/config.py`, `tests/unit/test_files.py` (added)

- `FileTools.execute` runs `list_files` / `find_in_file` (→ line numbers) / `read_file` (window + `total_lines`) under one root; `..`/absolute/symlink escapes rejected before I/O; per-call line + find caps; never raises (errors → strings).
- **Validation:** ruff pass · 11 new tests · full suite 662.

### LUMI-081: Bounded tool-loop in the LLMClient seam

**Status:** completed · **Commit:** 9a67183
**Files:** `core/llm.py`, `tests/unit/test_tool_loop.py` (added)

- `reply_structured` gains `tools`/`tool_executor`/`max_steps`; `AnthropicClient` loops (execute non-terminal tool → untrusted `tool_result` → repeat) until `set_state` or the cap (final round forces `set_state`); stats accumulate; no tools → unchanged. `MockLLMClient` gains `tool_script`.
- **Validation:** ruff pass · 7 new tests · full suite 669.

### LUMI-082: Wire the loop + file executor into Core.reply + config

**Status:** completed · **Commit:** 9ebebf6
**Files:** `core/agent.py`, `core/config.py`, `.env.example`, `.gitignore`, `tests/integration/test_file_tool.py` (added) + 3 test-fake signature fixes

- `Core.reply` runs the bounded loop over `files_dir/<user_id>` (fresh executor per turn, bound to the active user → isolation) when `LUMI_FILE_TOOL` is on; off → unchanged. `state/files/` gitignored.
- **Validation:** ruff pass · 3 new tests · full suite 672.

### LUMI-083: Safety bounds & graceful degradation

**Status:** completed · **Commit:** 65cd9d5
**Files:** `core/files.py`, `core/config.py`, `core/agent.py`, `.env.example`, `tests/unit/test_file_bounds.py` (added)

- Per-turn read-total budget (`LUMI_FILE_READ_MAX_TOTAL`): reads shrink to the remaining budget, then a "read limit reached" notice (fresh per turn). Loop cap forces a valid terminal reply; tool errors degrade to strings — the turn always completes.
- **Validation:** ruff pass · 5 new tests · full suite 677.

### LUMI-084: Contract + isolation + untrusted-content + behaviour tests

**Status:** completed · **Commit:** b85ebb9
**Files:** `tests/contract/test_file_tool_contract.py` (added)

- Per-user isolation (A can't read B's sandbox by name or traversal); **untrusted content end-to-end through the real loop** (a malicious file reaches the model wrapped as untrusted data and doesn't change the emotion); sandbox escapes denied through a full turn; the emotion contract validates with the loop active.
- **Validation:** ruff pass · 5 new tests · full suite **682**.

## Contracts honoured

- The emotion field `{reply, emotion, intensity}` is **unchanged** — `set_state` stays the terminal
  tool of every turn; the tool-loop only adds non-terminal read tools before it.
- **Sandboxed + untrusted + bounded + off by default**: paths confined to `files_dir/<user_id>`,
  `tool_result` framed as untrusted data, per-read/find/total caps + the loop cap, `LUMI_FILE_TOOL`
  off unless turned on.
- **Per-user isolation** extended to the file tool (pinned by the contract test).
- The bounded tool-loop is now the reusable foundation for v4.2 web search, v4.3 world context, v5.
- **No paid APIs in CI** — every backend/tool exercised through stubs/mocks.

## Next Steps

- v0.19 is feature-complete. Release when ready: `/release-version 0.19.0`.
- v0.20 (writing — `create_file` / `append_file`) rides on this same loop.
- No remaining issues under `v0::version:0`.
