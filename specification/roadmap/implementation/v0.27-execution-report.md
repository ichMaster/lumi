# Version v0.27 — Execution Report

**Date:** 2026-06-18
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.27 — Web lookup (Gemini grounded search) + the `/web` command
**Target version:** 0.27.0 (not released — awaiting confirmation)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-107 | core/weblookup.py — GeminiSearch seam + gemini_search + web_lookup tool + executor + config | v0.27 | completed | 2858448 | 3 | pass |
| 2 | LUMI-108 | Register web_lookup on Core.reply + the /web TUI command + safety + canon + docs | v0.27 | completed | 91f53e1 | 5 | pass |
| 3 | LUMI-109 | Contract + untrusted (EN+UK) + no-personal-data + caps + degradation + off-by-default tests | v0.27 | completed | 249d8a1 | 1 | pass |

## Detailed Results

### LUMI-107: core/weblookup.py — the GeminiSearch seam + web_lookup tool

**Status:** completed
**Commit:** 2858448
**Files changed:**
- `core/weblookup.py` (added) — `GeminiSearch` seam, `gemini_search` default (grounded `generateContent`, `tools:[{google_search:{}}]`, stdlib `urllib`, `GEMINI_API_KEY`), `_extract_answer` (answer-first), `web_lookup` schema + `WEB_LOOKUP_DIRECTIVE`, `WebLookupTools` executor (date-anchor, answer cap, per-turn call cap, never raises).
- `core/config.py` (modified) — `web_lookup` / `_model` / `_max_calls` (2) / `_max_chars` (2000); off by default; reuses `GEMINI_API_KEY`.
- `tests/unit/test_weblookup.py` (added) — 12 tests against a stub `GeminiSearch` + a monkeypatched `urlopen`.

**Validation:**
- [x] Unit tests: pass (12)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

---

### LUMI-108: register web_lookup on Core.reply + the /web command + docs

**Status:** completed
**Commit:** 91f53e1
**Files changed:**
- `core/agent.py` (modified) — `_web_tool_args` (fresh `WebLookupTools` per turn, injected/real `GeminiSearch`, clock date-anchor, per-turn caps); `_turn_tools` registration; the `WEB_LOOKUP_DIRECTIVE` prompt injection; `web_lookup_*` params + `build_core` threading.
- `tui/app.py` (modified) — the `/web` command (aliases `/search`, `/w`) routing through a normal turn; gated on `cfg.web_lookup`; the help hint.
- `.env.example` (modified) — the `LUMI_WEB_LOOKUP_*` block (off, paid, reuses `GEMINI_API_KEY`).
- `docs/WEB_LOOKUP_SETUP.md` (added) — operator guide.
- `tests/integration/test_web_tool.py` (added) — 4 full-turn integration tests (mock model + stub `GeminiSearch`).

**Validation:**
- [x] Integration tests: pass (4)
- [x] Full suite: pass (887)
- [x] Lint (ruff) + py_compile + TUI/core import: pass
- [x] Acceptance criteria: all pass

---

### LUMI-109: web lookup contract + safety tests

**Status:** completed
**Commit:** 249d8a1
**Files changed:**
- `tests/contract/test_web_contract.py` (added) — 10 tests: untrusted EN+UK (real AnthropicClient loop), no-personal-data, date-anchor, caps, degradation, fresh-per-turn counter, off-by-default, emotion contract.

**Validation:**
- [x] Contract tests: pass (10)
- [x] Full suite: pass (897)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

---

## Phase DoD (ROADMAP §v0.27)

- [x] With the flag on (+ a `GEMINI_API_KEY`), a turn (or `/web`) returns a fresh, grounded Ukrainian answer.
- [x] *"upcoming / this week"* anchors to **today** (the v0.4 clock).
- [x] An injection inside the answer (EN or UK) is ignored.
- [x] No personal/memory data appears in the outgoing query.
- [x] Per-turn + answer caps hold.
- [x] Off (default) → the tool + `/web` are absent.
- [x] The `{reply, emotion, intensity}` contract test passes verbatim.

## Next Steps

- All v0.27 issues complete; the full suite is green (897 passed). The version is **not** bumped — run
  `/release-version 0.27.0` when ready to cut the release.
- The **autonomous** twin (`%search` / `%events` thought-directives) lands later with the thought-tools
  phase (v0.32), per [TOOL_THOUGHTS.md](../../features/TOOL_THOUGHTS.md).
