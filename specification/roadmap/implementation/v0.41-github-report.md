# Version v0.41 — GitHub Issues Report

**Uploaded:** 2026-07-02
**Repository:** https://github.com/ichMaster/lumi
**Phase:** v0.41 — Model profiles: per-provider tier sets (`/model-set`)
**Total issues:** 4

## Issue Mapping

| LUMI ID | GitHub # | Title | Phase | Labels | URL |
|---------|----------|-------|-------|--------|-----|
| LUMI-160 | #160 | Profile config — `DEFAULT_MODEL_PROFILES` + the `LUMI_MODEL_PROFILES` parser | v0.41 | v0::version:0, v0::size:S, v0::area:core | https://github.com/ichMaster/lumi/issues/160 |
| LUMI-161 | #161 | `Core.switch_profile` — atomic engine + tier swap, active profile exposed | v0.41 | v0::version:0, v0::size:M, v0::area:core | https://github.com/ichMaster/lumi/issues/161 |
| LUMI-162 | #162 | `/model-set` TUI command — list/switch + status bar + MODELS_SETUP.md profiles | v0.41 | v0::version:0, v0::size:S, v0::area:core, v0::area:tui | https://github.com/ichMaster/lumi/issues/162 |
| LUMI-163 | #163 | `/model <full-id>` — bare-id provider inference by prefix | v0.41 | v0::version:0, v0::size:S, v0::area:core, v0::area:tui | https://github.com/ichMaster/lumi/issues/163 |

## Dependencies

- **#160** (profile config) is **independent** — the data layer; start first.
- **#163** (bare-id inference) is **independent** — runs in parallel with #160.
- **#161** (switch_profile) is **blocked by #160** — resolves from the profile config (comment posted).
- **#162** (/model-set) is **blocked by #161** — surfaces the seam (comment posted).

Build order: **(#160 ‖ #163) → #161 → #162**.

## Labels Created

None — all required labels already existed:
- v0::version:0
- v0::size:S, v0::size:M
- v0::area:core, v0::area:tui

## Notes

- **LUMI-160** = the data: three authored provider-homogeneous sets (anthropic Opus/Sonnet/Sonnet/Haiku;
  openai gpt-5.5/mini/mini/nano; gemini pro/flash/flash/flash-lite) + the `LUMI_MODEL_PROFILES` override
  parser (the `_parse_model_aliases` pattern — malformed skipped, merged onto defaults).
- **LUMI-161** = the headline: one atomic step re-points client + reply + all three v0.40 tier fields;
  a raising factory leaves everything untouched; routing now works on every engine (the provider guard
  stops mattering under a matching profile). Reply-only `/model` clears the active profile mark.
- **LUMI-162** = the surface: `/model-set` list/switch, the status-bar profile mark, the MODELS_SETUP.md
  profiles section.
- **LUMI-163** = quality-of-life on the v0.37 resolver: bare full ids infer their provider by prefix.
- **No contract change**; profiles unset → the v0.40 env-var behavior byte-identical; no paid calls in
  tests; nothing persists across restarts.
