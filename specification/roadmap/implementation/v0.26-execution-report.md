# Version v0.26 — Execution Report

**Date:** 2026-06-17
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.26 — Local dictation (STT)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All three v0.26 issues implemented, validated (STT **mocked** — no audio, no network, no paid calls),
committed, pushed, and closed in dependency order. The full suite grew from 847 → **865 tests** (+18). The
**core is untouched** — a dictated line is an ordinary `inbox` record (`source:"voice"` is reference only),
so the `{reply, emotion, intensity}` contract and per-user behaviour are unchanged.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-104 | `voice/stt.py` (STT adapter seam + `MockSTT`) + `voice/dictator.py` + config | v0.26 | completed | eba2a6f | 4 | pass |
| 2 | LUMI-105 | TUI listen-toggle (Ctrl+D) + drain `inbox` for dictation + the recognized line + docs | v0.26 | completed | 4bd9e10 | 6 | pass |
| 3 | LUMI-106 | Integration + contract — recognized line → `inbox` → a turn identical to typed | v0.26 | completed | 70340da | 1 | pass |

(+ `c35981b` — the `dictation` optional-dependency extra, completing the dictator's runnability.)

## Detailed Results

### LUMI-104: the STT adapter seam + the dictator process

**Status:** completed · **Commit:** eba2a6f
**Files changed:**
- `voice/stt.py` (new) — the `/voice` STT seam (mirror of `tts.py`): an `STT` Protocol
  (`recognize(audio, *, lang) -> str`), cloud impls `DeepgramSTT` (Nova-3 uk) + `ElevenLabsScribeSTT` and
  **offline** `WhisperSTT` (all **lazy** SDK imports — the module imports with no STT dep), `MockSTT`, and
  a `build_stt(provider)` factory.
- `voice/dictator.py` (new) — the voicer's mirror (`mic → inbox`): `read_flag`, `recognize_and_append`
  (append `{id, text, source:"voice", ts}`; **empty/low-confidence + errors → a dropped line, never
  raises**; dedup via the fifo id), and the `run()` glue (watch `listen.flag`, record, recognize — uncovered).
- `core/config.py` — `LUMI_DICTATION` / `LUMI_STT_PROVIDER` / `LUMI_STT_LANG` / `LUMI_LISTEN_FLAG` / `DEEPGRAM_API_KEY`.
- `tests/unit/test_stt.py` — 9 unit tests (MockSTT, no audio/network).

**Validation:** unit 9 pass · ruff/compile pass · full suite 856 pass. No core change.

---

### LUMI-105: the TUI listen toggle + inbox drain for dictation + docs

**Status:** completed · **Commit:** 4bd9e10
**Files changed:**
- `tui/bridge.py` — `drain_inbox_records` (records, so the TUI tags by `source`; `drain_inbox` is now a
  text-only convenience over it) + `set_listen_flag` (the TUI is the **sole** writer of `listen.flag`).
- `tui/app.py` — a **Ctrl+D** binding → `action_toggle_listen`; `on_mount` generalizes the inbox drain
  from **bridge-only to bridge-OR-dictation** (the `_poll_inbox` gate + the path/interval wiring);
  `_drain_inbox` tags a `source="voice"` line as **🎤 ти** vs a 📱 Telegram line — either runs a turn
  identical to typed.
- `.env.example` + `docs/DICTATION_SETUP.md` (the three processes, the toggle, providers incl. offline
  Whisper); the stale **v0.21 → v0.26** refs in `DICTATION_LOCAL.md` fixed.
- `tests/unit/test_dictation_bridge.py` — 4 unit tests.

**Validation:** unit 4 pass · `tui.app` imports · ruff/compile pass · full suite 860 pass. No core change.

---

### LUMI-106: integration + contract

**Status:** completed · **Commit:** 70340da
**Files changed:**
- `tests/integration/test_dictation.py` — 5 tests (mock STT, no audio, no Textual app): a recognized line
  drives a turn **identical to typed**; empty/low-confidence → no record + no turn; dedup (consumed once);
  dictation works with **no bridge**; the emotion contract + `[user, lili]` persistence hold.

**Validation:** integration 5 pass · ruff pass · **full suite 865 pass** — no audio, no network, no paid calls.

## DoD check (ROADMAP §v0.26)

- [x] Press the listen key, speak Ukrainian, and the recognized line appears (voice-marked) and is answered
      — identically to typing it (the core can't tell typed from dictated).
- [x] A missed / empty utterance is dropped, not garbled into the chat; dedup by id.
- [x] Dictation toggles without touching the core; STT mocked (no paid call); independent of the bridge.

## Next Steps

The v0.26 phase is complete (3/3 issues). The version has **not** been bumped — cut the release with
`/release-version 0.26.0` when ready. The next roadmap phase is **v0.27 — Semantic recall III: chunking
long messages**. The web sibling of dictation is **v3.4** (the same `/voice` STT adapter).

**Note — remaining cross-ref drift:** `DICTATION_LOCAL.md` is now fixed (v0.26), but MISSION/ARCHITECTURE/
CLAUDE/FILE_THOUGHTS still cite dictation as v0.21/v0.22 — a docs-only cleanup pass is still pending.
