# Version v0.30 — Execution Report

**Date:** 2026-06-18
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.30 — Semantic recall III: chunking long messages
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All four issues (LUMI-116 → LUMI-117 → LUMI-118 → LUMI-119) implemented, validated, committed, pushed, and
closed in dependency order. The full suite grew from **942 → 967** tests (+25), all green.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-116 | The chunker (core/chunking.py) + LUMI_RAG_CHUNK* config | v0.30 | completed | 15f93b6 | 3 | pass |
| 2 | LUMI-117 | VectorRecord chunk fields + chunk-granular indexing + staleness tag + contract test | v0.30 | completed | 881dc85 | 5 | pass |
| 3 | LUMI-118 | Two-level expansion (chunk-window inside the message-window) + /recall reuse | v0.30 | completed | c964627 | 2 | pass |
| 4 | LUMI-119 | Chunking isolation contract + behaviour/degradation tests | v0.30 | completed | 351df02 | 1 | pass |

## Detailed Results

### LUMI-116: The chunker + LUMI_RAG_CHUNK* config

**Status:** completed
**Commit:** 15f93b6
**Files changed:**
- `core/chunking.py` (new) — pure `chunk_text(text, *, chunk_chars, overlap, threshold)`: one chunk at/below
  threshold (v0.16), else ~`chunk_chars` passages broken at the latest paragraph/sentence/word boundary in
  the window's second half (no tiny/mid-word chunks) with a small overlap; a single huge token hard-cuts.
  Deterministic, order-preserving, full coverage; never raises.
- `core/config.py` (modified) — `rag_chunk` (`LUMI_RAG_CHUNK`, off) + `rag_chunk_chars`/`_overlap`/`_threshold`/`_w`.
- `tests/unit/test_chunking.py` (new) — 9 unit tests.

**Validation:** unit (9) pass · full suite 951 pass · ruff + py_compile pass · acceptance all pass.

---

### LUMI-117: VectorRecord chunk fields + chunk-granular indexing + staleness tag

**Status:** completed
**Commit:** 881dc85
**Files changed:**
- `core/repository.py` — `VectorRecord` gains `parent_msg_id` + `chunk_index` (additive; `__post_init__`
  defaults `parent_msg_id` to `msg_id` → back-compat); new `chunk_msg_id(parent, index, text)`.
- `core/agent.py` — `_index_messages`/`backfill_vectors` route through `_embed_and_build`/`_chunks_for`
  (chunk on → one record per chunk; off → one per message); `_message_indexed` keys idempotency off the
  first chunk id; the `build_core` staleness tag appends `@chunk<chars>` when on.
- `tests/integration/test_recall_index.py` — 5 chunk-indexing tests (+ the existing 25).
- `tests/contract/test_memory_records.py` — `VectorRecord` shape updated (+ chunk-fields test).
- `specification/ARCHITECTURE.md` — §Semantic recall documents the chunk fields + chunk-id rule (contract change, same commit).

**Validation:** targeted (recall index + memory-records contract) 30 pass · full suite 957 pass · ruff + py_compile pass · acceptance all pass.

---

### LUMI-118: Two-level expansion + /recall reuse

**Status:** completed
**Commit:** c964627
**Files changed:**
- `core/agent.py` — hits resolve via `parent_msg_id`; new `_passage_text` (matched chunk ± `chunk_w`
  adjacent chunks, de-overlapped, gaps/ends elided); `_expand_hits` renders a long anchor as its passage
  inside the ±`rag_w` thread (neighbours whole); anchor dedup keys off `parent_msg_id`; `recall_moments`
  (`/recall`) reuses it; off → byte-identical to v0.17.
- `tests/integration/test_recall_chunking.py` (new) — 5 expansion tests.

**Validation:** chunking expansion (5) pass · full suite 962 pass · ruff + py_compile pass · acceptance all pass.

---

### LUMI-119: Isolation contract + behaviour/degradation tests

**Status:** completed
**Commit:** 351df02
**Files changed:**
- `tests/contract/test_semantic_recall_isolation.py` — v0.30 section (5 tests): chunked recall is
  single-user (recall level + the per-turn RAG block); short-message block byte-identical on vs off;
  embedder-error graceful degradation; the emotion contract holds with chunked recall.

**Validation:** isolation contract (15) pass · full suite 967 pass · ruff pass · acceptance all pass.

## Notes

- **Contract change (LUMI-117)** landed correctly: `VectorRecord` gained `parent_msg_id` + `chunk_index`,
  and the memory-records contract test + ARCHITECTURE §Semantic recall were updated **in the same commit**.
- **Off by default, fully back-compatible:** with `LUMI_RAG_CHUNK` off, one vector per message and the
  recall block are byte-identical to v0.16/0.17 (a one-chunk message keeps the message id as its `msg_id`).
  The `Embedder`/`VectorStore` seams and the `{reply, emotion, intensity}` contract are untouched.
- **Isolation extended over chunks:** search, the chunk window, and the message window all run only over
  the requesting user's data (pinned A↔B).

## Next Steps

- The v0.30 phase is complete. The version has **not** been bumped — release via `/release-version 0.30.0`
  when desired.
- Next roadmap phase: **v0.31 — Semantic recall IV: thematic recall (topic routing)**.
