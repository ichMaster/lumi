# Version v0.1 — Execution Report

**Date:** 2026-06-06
**Branch:** main
**Label:** v0::version:0
**Target version (phase v0.1 → semver):** 0.1.0 — **not yet released** (awaiting confirmation)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 6 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All v0.1 issues implemented in dependency order, each validated (`pytest` + `ruff`,
mock model — **no paid APIs**), one commit per issue, pushed to `main`, issue closed.
Final test suite: **34 passed**; `ruff check .` clean.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-001 | Project skeleton & tooling | v0.1 | completed | 0d2efea | 9 | pass |
| 2 | LUMI-002 | LLMClient seam + Anthropic Claude Haiku | v0.1 | completed | f4bb9a1 | 2 | pass |
| 3 | LUMI-003 | Canon loading & system-prompt assembly | v0.1 | completed | 2e570b8 | 3 | pass |
| 4 | LUMI-004 | Repository interface + local store | v0.1 | completed | 509209d | 3 | pass |
| 5 | LUMI-005 | Core turn — reply(...) orchestration | v0.1 | completed | 728f88d | 5 | pass |
| 6 | LUMI-006 | TUI loop (Textual) | v0.1 | completed | 1aa15bc | 3 | pass |

(Planning docs committed separately in `bb2f1df`.)

## Detailed Results

### LUMI-001: Project skeleton & tooling

**Status:** completed · **Commit:** 0d2efea
**Files changed:** `pyproject.toml`, `uv.lock`, `.env.example`, `README.md`,
`core/__init__.py`, `core/config.py`, `state/__init__.py`, `tui/__init__.py`,
`tests/unit/test_config.py`

**Validation:**
- [x] Unit tests: pass (`test_config.py`)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

Notes: uv-managed project (flat layout); ruff line-length 100 / `E,F,I,W,UP,B` /
ignore E501; pytest `testpaths`/`pythonpath`. `core.config` loads `.env`, exposes
the model id (default Claude Haiku), canon path, store path, memory-window placeholder.

---

### LUMI-002: LLMClient seam + Anthropic Claude Haiku

**Status:** completed · **Commit:** f4bb9a1
**Files changed:** `core/llm.py`, `tests/unit/test_llm.py`

**Validation:**
- [x] Unit tests: pass (9; protocol conformance, canned mock, bounded retry, no network)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

Notes: `LLMClient` Protocol; `AnthropicClient` (SDK imported only inside the class);
`MockLLMClient`; `_call_with_retries` (bounded, unit-tested) → `LLMError`, never hangs.

---

### LUMI-003: Canon loading & system-prompt assembly

**Status:** completed · **Commit:** 2e570b8
**Files changed:** `core/canon/lili.md`, `core/prompt.py`, `tests/unit/test_prompt.py`

**Validation:**
- [x] Unit tests: pass (6; assembly, missing/empty errors, all-9-emotions palette)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

Notes: ~695-word Ukrainian canon per CANON_SPEC. Loader module named `prompt.py`
to avoid colliding with the `core/canon/` asset directory. `build_system_prompt`
returns the canon verbatim — the seam v0.2/v0.3/v0.5 extend.

---

### LUMI-004: Repository interface + local store

**Status:** completed · **Commit:** 509209d
**Files changed:** `core/repository.py`, `state/local_store.py`, `tests/unit/test_repository.py`

**Validation:**
- [x] Unit tests: pass (8; round-trip, restart reload, role validation, no user_id yet)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

Notes: `Repository` Protocol + `Session`/`Message` shapes (no `user_id` until v0.2);
`JsonRepository` atomic-write store; history reloads across a restart.

---

### LUMI-005: Core turn — reply(...) orchestration

**Status:** completed · **Commit:** 728f88d
**Files changed:** `core/agent.py`, `core/config.py` (+`store_path`),
`tests/integration/test_turn.py`, `tests/unit/test_config.py`, `.gitignore`

**Validation:**
- [x] Integration test: pass (turn → reply via MockLLMClient; both messages persisted;
  canon+history sent to the model; restart reload; `build_core` with injected mock)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

Notes: `Core.reply(user_text, session)` is the single public contract; `core` reaches
the model only via `LLMClient`. Full session history in context (trimming is v0.2).

---

### LUMI-006: TUI loop (Textual)

**Status:** completed · **Commit:** 1aa15bc
**Files changed:** `tui/app.py`, `tui/__main__.py`, `tests/integration/test_tui.py`

**Validation:**
- [x] Integration test: pass (3; Textual pilot turn, empty-input ignored, failure degradation)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: pass (one **manual** item below)

Notes: `LumiApp` calls `core.reply` off the UI thread (`asyncio.to_thread`); a model
error degrades to a readable line, never a crash. `python -m tui` wires the real core
and exits clearly without a key. **Manual, not run in CI:** a real Claude Haiku turn
with `ANTHROPIC_API_KEY` set (interactive terminal + paid key).

## Phase DoD (ROADMAP §v0.1)

> You can hold a dialogue with Лілі in the terminal (Claude Haiku); she keeps her
> character within a session.

Met in code and tests (mock model). The end-to-end paid-model dialogue is the one
manual verification step (run `uv run python -m tui` with `ANTHROPIC_API_KEY` set).

## How to run

```bash
uv sync --extra dev
uv run ruff check .        # clean
uv run pytest              # 34 passed
uv run python -m tui       # needs ANTHROPIC_API_KEY in .env
```

## Next Steps

- **Optional release:** version is **not** bumped automatically. To cut `0.1.0`,
  run `/release-version 0.1.0` (updates VERSION/README/RELEASE.txt, tags, pushes).
- **Manual check:** run the TUI with a real `ANTHROPIC_API_KEY` to confirm the
  live Claude Haiku dialogue (LUMI-006 manual criterion).
- **Next phase:** v0.2 — Three-layer memory (introduces `User`/`user_id`, rolling
  window, `ShortSummary`/`LongTermFact`, memory view/clear). Break it down with
  `/upload-issues specification/roadmap/implementation/v0.2-issues.md`.
