# Version v0 (phase v0.15) — Execution Report

**Date:** 2026-06-12
**Branch:** v0.12-dev
**Label:** v0::version:0 (phase v0.15 — Prompt caching)
**Target version:** 0.15.0 (not released — awaiting confirmation)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Full suite: **512 pass** (was 498 at the phase start; +14). No network, no paid calls — the
Anthropic adapter is exercised via an injected fake client; the toggle is gated in the core and the
mock backend ignores the hint. **No core contract change** (the emotion field + memory records are
untouched; `cache_prefix` is an additive, ignorable seam hint).

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-066 | Reorder `build_system_prompt` — stable prefix vs per-turn tail | v0.15 | completed | 792bce7 | 12 | pass |
| 2 | LUMI-067 | Cache breakpoint in the Anthropic adapter + `LUMI_PROMPT_CACHE` | v0.15 | completed | f967fc2 | 7 | pass |
| 3 | LUMI-068 | Cache accounting — `ResponseStats` + status line | v0.15 | completed | 523c440 | 4 | pass |
| 4 | LUMI-069 | Caching contract test — prefix stability + per-turn isolation | v0.15 | completed | d98b5f5 | 1 | pass |

## Detailed Results

### LUMI-066 — Reorder build_system_prompt (#61)

**Status:** completed · **Commit:** 792bce7
- `build_system_prompt` returns `(system, cache_prefix)`: stable prefix (canon + emotion/relation
  instructions + memory/facts-digest + mood) then per-turn tail (ambient `# Зараз` + closeness +
  thoughts + the style palette). **Only the ambient block moves** (it was early); closeness/thoughts/
  style were already late. **Style stays the very last block** (a design decision — kept salient,
  ~396 tok, instead of the cached prefix; user-confirmed). `system.startswith(cache_prefix)` holds.
- `_system_prompt` returns the tuple; `reply()` + the proactive-think path + 6 test files updated;
  a new unit test pins the prefix/tail split. Spec aligned (ROADMAP §v0.15, PROMPT_OPTIMIZATION,
  v0.15-issues → style-in-tail).
- **Validation:** unit + integration 499 pass, ruff, compile — pass.

### LUMI-067 — Cache breakpoint + LUMI_PROMPT_CACHE (#62)

**Status:** completed · **Commit:** f967fc2
- `LLMClient.reply`/`reply_structured` gain an optional `cache_prefix`. `AnthropicClient._base_kwargs`
  emits `system` as `[prefix(cache_control:ephemeral), remainder]` (concatenates to the original) when
  a valid prefix is given; unset / not-a-prefix → plain string (byte-identical). Whole-system prefix →
  one cached block.
- `LUMI_PROMPT_CACHE` (on by default) → `Core._prompt_cache`; `reply()` passes `cache_prefix` on the
  reply turn only (housekeeping passes none). `MockLLMClient` + the `_ThinkingRecorder` stub ignore it.
- **Validation:** unit (block shape on/off, whole-system, not-a-prefix fallback, mock-ignores) + config
  parse; full suite 506 pass; ruff — pass.

### LUMI-068 — Cache accounting (#63)

**Status:** completed · **Commit:** 523c440
- `ResponseStats` gains `cache_write_tokens` (from `usage.cache_creation_input_tokens`), populated
  beside `cache_read_tokens`. The TUI stats line shows `cache <n>↩` (served) and `wrote <n>↑` (written).
- **Validation:** unit (capture from a faked usage + `LumiApp._stats_text` render); full suite 508 pass; ruff — pass.

### LUMI-069 — Caching contract test (#64)

**Status:** completed · **Commit:** d98b5f5
- `tests/contract/test_prompt_cache.py`: prefix byte-identical across turns while the tail changes;
  per-turn blocks never enter the prefix; the toggle controls whether `cache_prefix` is passed; the
  mock replies identically with caching on vs off.
- **Validation:** 4 contract tests; full suite **512 pass**; ruff — pass.

## DoD check (ROADMAP §v0.15)

- [x] Within a session the **prefix** (canon + instructions + day-stable memory + facts-digest + mood)
      is byte-identical turn-to-turn and carries one `cache_control` breakpoint.
- [x] Per-turn blocks (ambient time, the closeness block, thoughts, the style palette) and the
      messages sit after it.
- [x] With caching on, a warm turn reports `cache_read` tokens; off → the same prompt text, no marker.
- [x] **Provider-agnostic** (mock + future backends ignore the hint); **mocked in every test**
      (no network / paid calls); **no content, character, or contract change**.
- [x] Relies on the facts digest to keep the prefix stable.

## How to verify it live

1. `.env`: `LUMI_PROMPT_CACHE=on` (default). Restart the TUI.
2. Send a few messages — the **first** turn shows `wrote <n>↑` in the stats line (cache write); each
   **warm** turn after (within 5 min) shows `cache <n>↩` (prefix served from cache, billed ~10%).

## Next Steps

- v0.15 is **code-complete and green** but **not released**. Cut **0.15.0** via `/release-version 0.15.0`
  when ready.
- Next roadmap phase: **v0.16 — Semantic recall I (RAG)** (issues LUMI-061…065 drafted in
  `v0.16-issues.md`, not yet uploaded).
