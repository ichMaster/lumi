# Version v0 (phase v0.9) — Execution Report

**Date:** 2026-06-08
**Branch:** main
**Label:** v0::version:0
**Phase:** v0.9 — Richer short memory (recent detail + days at a glance)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-034 | Two-tier `ShortSummary` — detailed + gist (one call) + migration | v0.9 | completed | 905c299 | 8 | pass |
| 2 | LUMI-035 | Repository — summaries within the last D local days | v0.9 | completed | 35b4fe2 | 3 | pass |
| 3 | LUMI-036 | Two-tier short-memory injection (last N detailed + D-day gists) | v0.9 | completed | f439585 | 4 | pass |

Final suite: **289 passed**, ruff clean. No paid API calls (model mocked throughout).

## Detailed Results

### LUMI-034: Two-tier ShortSummary — detailed + gist (one call) + migration

**Status:** completed · **Commit:** 905c299

- `ShortSummary{user_id, session_id, summary, gist, ts}` — added the one-line `gist` tier.
- `SUMMARY_SYSTEM` asks for a detailed summary + a trailing «СТИСЛО:» line; `parse_summary(text) →
  (detailed, gist)` splits them (gist falls back to the first sentence). One end-of-session call.
- `Core._write_summary` parses + persists both. JSON store migration: pre-v0.9 records load with
  `gist=""`. Updated the `ShortSummary` **contract test**; `LongTermFact` untouched.

**Validation:** parse_summary (marker + fallback), end_session writes both tiers, migration, updated
contract shape; ruff; all criteria pass.

### LUMI-035: Repository — summaries within the last D local days

**Status:** completed · **Commit:** 35b4fe2

- `Repository.summaries_since(user_id, since_date)` + JSON impl — a user's summaries whose `ts`
  date is on/after a `YYYY-MM-DD` date (date-prefix compare), newest last, **user-scoped**.
  `recent_summaries` stays the recency query.

**Validation:** window range + per-user isolation; ruff; criteria pass.

### LUMI-036: Two-tier short-memory injection (last N detailed + D-day gists)

**Status:** completed · **Commit:** f439585

- `Core._system_prompt` injects two tiers: the last **N=5** conversations in detail + all
  conversations in the last **D=5 local days** as one-line gists — recent N **not repeated**
  (dedup), empty (pre-v0.9) gists skipped, **capped** (`GIST_CAP=20`), all dated; the D-day window
  uses the injected clock. `build_system_prompt` gained a `gists` block. Long-term facts unchanged.

**Validation:** two-tier assembly, dedup, D-day window + beyond-window exclusion, empty-gist
omission, no-gist-tier-when-empty (fake store + fixed clock); ruff; criteria pass.

## Phase DoD

Met ([ROADMAP.md §v0.9](../../ROADMAP.md)): the prompt carries the **last 5 conversations in detail**
and the **last 5 days' conversations as gists** (no duplication, dated, capped); each session's
summary is two-tiered from **one** call; **old summaries still load and inject** (migration);
**long-term facts unchanged**; per-user isolation holds. ARCHITECTURE §Memory + the `ShortSummary`
contract test are in sync.

## Next Steps

- All v0.9 issues complete. **Version not bumped** (awaiting confirmation) — cut with
  `/release-version 0.9.0` when ready.
- Next phase: **v0.10 — Closeness (relationship level)**.
