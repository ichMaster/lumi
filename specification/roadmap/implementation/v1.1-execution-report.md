# Version v1.1 — Execution Report

**Date:** 2026-07-13
**Branch:** v1.1-dev
**Label:** v1::version:1
**Phase:** v1.1 — Conversation moves: the anti-mirror engine (retrospective + arbiter in the think-phase)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 5 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-175 | The `move` field — `set_state` + the `Message` record + config | v1.1 | completed | c9abb90 | 9 | pass (1418) |
| 2 | LUMI-176 | History with declared types | v1.1 | completed | 2c50f4b | 3 | pass (1422) |
| 3 | LUMI-177 | Arbiter dynamics in code — `{move_rules}` via the resolver | v1.1 | completed | fcab892 | 4 | pass (1431) |
| 4 | LUMI-178 | `inner_voice.md` v2 + canon audit | v1.1 | completed | 480c113 | 5 | pass (1437) |
| 5 | LUMI-179 | Integration & DoD — the rule-walk + byte-identical off | v1.1 | completed | 845c6db | 3 | pass (1440) |

## Detailed Results

### LUMI-175: The `move` field

**Status:** completed · **Commit:** c9abb90
**Files:** `core/moves.py` (new), `core/repository.py`, `core/prompt.py`, `core/llm.py`, `core/config.py`, `core/agent.py`, tests ×3

- The closed English enum (`deepen/position/object/develop/associate/example/return` —
  the emotion-enum precedent; UA names are display labels) + `validate_move` (unknown → None, silent).
- The additive optional `move` on `set_state`: the Anthropic tool + Gemini responseSchema properties,
  the gated `MOVE_INSTRUCTION` in the prompt (OpenAI JSON mode rides the instruction — per-provider
  handling, the v0.10 `relation` pattern).
- `Message.move` beside `emotion`/`intensity` (None default → pre-v1.1 stores load without migration);
  stored on Лілі's lines only; the message-record contract test now pins the shape.
- `LUMI_MOVES` (off by default).

### LUMI-176: History with declared types

**Status:** completed · **Commit:** 2c50f4b
**Files:** `core/agent.py`, `core/prompt.py`, tests

- `_history_content` re-appends `<move>…</move>` to Лілі's replayed lines from the persisted field —
  replay-only metadata (stored text stays clean; untyped records replay byte-identically).
- `split_move` strips any imitated marker from replies (the type can never leak to a renderer or the
  outbox) and doubles as the fallback channel when the structured tool can't be forced (extended
  thinking — the emotion-tag precedent); the structured field wins; off ignores the fallback.

### LUMI-177: Arbiter dynamics in code

**Status:** completed · **Commit:** fcab892
**Files:** `core/moves.py`, `core/agent.py`, tests ×2

- Pure `arbiter_dynamics(window)`: declared-type lines, the same-type hard ban (declared twice in a
  row → not a third time), the topic-died hint (two reactions ≤ `SHORT_REACTION_CHARS`); `""` on no
  data / bad input — never raises, never blocks.
- Computed over the live window each turn; `{move_rules}` resolves via the v0.12 resolver only when
  `LUMI_MOVES` is on and the token is present — off → no resolve pass (the token stays literal).
- Execution judgment (`виконано: так/ні`) stays with the model's retrospective — code substitutes
  only what data shows.

### LUMI-178: `inner_voice.md` v2 + canon audit

**Status:** completed · **Commit:** 480c113
**Files:** `core/inner_voice_moves.md` (new, authored), `core/canon/lili.md`, `core/config.py`, `core/agent.py`, tests

- The four-block v2 think instruction: `[ретроспектива]` (declared-vs-done validation; dry binary
  questions, no self-shaming) → `[голоси]` (Імпульс/Тверезість/Стандарт each proposing a typed move)
  → `[арбітр]` (the full rule table + emergency mode + `{move_rules}`) → `[репліка]` (performs the
  move + sets the field); the five v0.38 hard rules restated.
- `build_core`: moves on → the v2 file (missing → the v0.38 chain, logged); off → untouched.
  Config: `inner_voice_moves_path` / `LUMI_INNER_VOICE_MOVES_FILE`.
- Canon audit: three few-shot mirror/move contrast pairs added to §Як тече розмова; the behavioral
  anti-mirror rules were already authored (§ЗАБОРОНИ, §Як тече розмова) — the missing lever was the
  contrast pairs.

### LUMI-179: Integration & DoD

**Status:** completed · **Commit:** 845c6db
**Files:** `tests/integration/test_moves_rule_walk.py` (new), `core/agent.py`, `docs/MOVES_SETUP.md` (new)

- The scripted 20-exchange rule-walk over the shipped v2 instruction: per-turn stored `move` sequence,
  bans landing on the turn after each double-declare, the topic-died hint after terse reactions,
  no 3-in-a-row types, no marker leaks anywhere.
- Off-pin hardened: `_history_content` gated on the flag — a store typed during an on-period replays
  with **no** move artifacts once off (instruction/marker/value all gone).
- All seven enum values round-trip the store. Operator note: `docs/MOVES_SETUP.md`.

## Validation (final)

- **pytest: 1440 passed** (47 new across the phase), no paid calls (MockLLMClient throughout).
- **ruff: clean.**
- Contracts: `{reply, emotion, intensity}` untouched (emotion-channel tests verbatim); the
  message-record contract extended in the same phase as the schema change; per-user isolation
  untouched; **off → byte-identical** pinned at every layer (prompt, replay, store, directive).

## Next Steps

The v1.1 phase is complete — all 5 issues closed. Release (`/release-version 1.1.0`) awaits the
user's explicit confirmation. The natural next phase is v1.2 — Topic base (the `associate`/`return`
moves' own material).
