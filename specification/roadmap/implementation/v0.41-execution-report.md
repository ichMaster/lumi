# Version v0.41 — Execution Report

**Date:** 2026-07-02
**Branch:** v0.39-dev
**Label:** v0::version:0
**Phase:** v0.41 — Model profiles: per-provider tier sets (`/model-set`)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-160 | Profile config — `DEFAULT_MODEL_PROFILES` + the `LUMI_MODEL_PROFILES` parser | v0.41 | completed | c833a6e | 2 | pass (1324) |
| 2 | LUMI-163 | `/model <full-id>` — bare-id provider inference by prefix | v0.41 | completed | d2b501a | 4 | pass (1327) |
| 3 | LUMI-161 | `Core.switch_profile` — atomic engine + tier swap | v0.41 | completed | b0e8d2c | 2 | pass (1332) |
| 4 | LUMI-162 | `/model-set` TUI command + status bar + docs | v0.41 | completed | 86cb740 | 3 | pass (1336) |

## Detailed Results

### LUMI-160: Profile config

**Status:** completed · **Commit:** c833a6e
**Files:** `core/config.py`, `tests/unit/test_model_profiles.py` (new)

- `ModelProfile` NamedTuple `{provider, reply, think, mood, housekeeping}` — one `provider` per profile
  (structural homogeneity); `DEFAULT_MODEL_PROFILES` (anthropic Opus/Sonnet/Sonnet/Haiku; openai
  gpt-5.5/mini/mini/nano; gemini 3.1-pro-preview/flash/flash/flash-lite); the `LUMI_MODEL_PROFILES`
  parser (`;`-separated, malformed skipped, merged onto defaults). 6 tests.

**Validation:** ✅ pytest · ✅ ruff · ✅ acceptance criteria (all)

### LUMI-163: `/model` bare-id inference

**Status:** completed · **Commit:** d2b501a
**Files:** `core/agent.py`, `tui/app.py`, `docs/MODELS_SETUP.md`, `tests/unit/test_model_switch.py`

- `_MODEL_ID_PREFIXES` + a third step in `resolve_model_target` (after aliases + `provider:id`):
  `claude-*`→anthropic, `gpt-*`/`o1|o3|o4`→openai, `gemini-*`→gemini, `deepseek-*`→deepseek; unknown
  prefixes keep the clear reject, now with the full-id hint. Help text + docs. 3 tests.

**Validation:** ✅ pytest · ✅ ruff · ✅ acceptance criteria (all)

### LUMI-161: `Core.switch_profile`

**Status:** completed · **Commit:** b0e8d2c
**Files:** `core/agent.py`, `tests/unit/test_model_profiles.py`

- `switch_profile(name)`: resolve (unknown → clear `ValueError`) → `switch_model` (client built first —
  atomic on a raising factory) → re-point the three v0.40 tier fields → set the active profile.
- `profile` + `model_profiles` properties; the reply-only `switch_model` clears the profile mark.
- **Guard update:** under an active profile the anthropic-only guard steps aside (tiers homogeneous by
  construction) — routing now works on every engine; raw env-var mode keeps the v0.40 guard. 5 tests.

**Validation:** ✅ pytest · ✅ ruff · ✅ acceptance criteria (all)

### LUMI-162: `/model-set` + status bar + docs

**Status:** completed · **Commit:** 86cb740
**Files:** `tui/app.py`, `docs/MODELS_SETUP.md`, `tests/unit/test_model_profiles.py`

- `/model-set` lists (active marked) / switches with the `/model` error style; status bar shows
  `profile:model` while active, mark dropped by a reply-only `/model`; dispatch collision-free (pinned
  both directions by a Textual pilot test); help line + the MODELS_SETUP.md profiles section. 4 pilot
  tests.

**Validation:** ✅ pytest (full suite, **1336 passed**) · ✅ ruff · ✅ acceptance criteria (all)

## Next Steps

- All v0.41 issues are done; no open issues remain. Release `0.41.0` via `/release-version` when you
  decide — the version is **not** bumped automatically.
- Optional operator follow-up: with profiles live, `/model-set gemini` now keeps routing on — the
  cache report will show per-kind Gemini tiers if you try it.
