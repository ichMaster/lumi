# Version v0.28 — Execution Report

**Date:** 2026-06-18
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.28 — Journal tool (day-summary diary + read-by-date, auto-stamped mood/biorhythm/forecast)
**Target version:** 0.28.0 (not released — awaiting confirmation)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-110 | core/journal.py — JournalTools executor (code-owned stamp) + journal_write/read/list + config | v0.28 | completed | e482a97 | 3 | pass |
| 2 | LUMI-111 | Register the journal tools on Core.reply + the /journal command + safety + docs | v0.28 | completed | c609795 | 5 | pass |
| 3 | LUMI-112 | Contract + code-owns-metadata + non-destructive + per-user isolation + reread-untrusted tests | v0.28 | completed | a5b4745 | 1 | pass |

## Detailed Results

### LUMI-110: core/journal.py — the JournalTools executor + the three tools

**Status:** completed
**Commit:** e482a97
**Files changed:**
- `core/journal.py` (added) — `JOURNAL_TOOLS` (3 schemas; `journal_write`'s only arg is `text`) + `JOURNAL_DIRECTIVE`; the `JournalTools` executor over a per-user sandbox root (code-owned header from the injected `stamp`, non-destructive create/append via `safe_path`, read-by-date / list, never raises).
- `core/config.py` (modified) — `journal` / `journal_dir` / `journal_max_chars`; off by default.
- `tests/unit/test_journal.py` (added) — 13 tests (injected stamp + temp sandbox).

**Validation:**
- [x] Unit tests: pass (13)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

---

### LUMI-111: register the journal tools on Core.reply + the /journal command

**Status:** completed
**Commit:** c609795
**Files changed:**
- `core/agent.py` (modified) — `_journal_stamp` (code-owned mood/biorhythm/forecast blockquote from `self._mood` + `self._biorhythms` — the v0.8 "code, not model" merge); `_journal` + `_journal_tool_args` (fresh per-turn, per-user sandbox, clock date/time); `_turn_tools` registration; the `JOURNAL_DIRECTIVE` prompt injection; `Core.journal_read` / `journal_list` helpers; `journal_*` params + `build_core` threading.
- `tui/app.py` (modified) — the `/journal [date|list|write]` command + the flag + help hint.
- `.env.example` (modified) — the `LUMI_JOURNAL_*` block.
- `docs/JOURNAL_SETUP.md` (added) — operator guide.
- `tests/integration/test_journal_tool.py` (added) — 5 full-turn integration tests (mock model + injected `MoodState`).

**Validation:**
- [x] Integration tests: pass (5)
- [x] Full suite: pass (915)
- [x] Lint (ruff) + py_compile + TUI/core import: pass
- [x] Acceptance criteria: all pass

---

### LUMI-112: journal contract + safety tests

**Status:** completed
**Commit:** a5b4745
**Files changed:**
- `tests/contract/test_journal_contract.py` (added) — 9 tests: code-owns-metadata, non-destructive, per-user isolation, reread-untrusted (EN+UK, real AnthropicClient loop), off-by-default, missing-date / mood-off degradation, emotion contract.

**Validation:**
- [x] Contract tests: pass (9)
- [x] Full suite: pass (924)
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

---

## Phase DoD (ROADMAP §v0.28)

- [x] With the flag on, `journal_write` creates today's dated file with the **code-stamped** mood/biorhythm/forecast header (matching `/mood` + `/biorhythm`) + her prose.
- [x] A second same-day write **appends** (never overwrites); `journal_read` / `journal_list` return entries by date.
- [x] The path is sandboxed, code-fixed from the clock, and per-user isolated (A can't read B's journal).
- [x] A reread entry's embedded "ignore your instructions" is ignored; every path returns a string (never raises).
- [x] Off (default) → the tools + `/journal` are absent.
- [x] The `{reply, emotion, intensity}` contract test passes verbatim.

## Next Steps

- All v0.28 issues complete; the full suite is green (924 passed). The version is **not** bumped — run
  `/release-version 0.28.0` when ready to cut the release.
- The grander admin-panel / gallery / mood-drawing literary form of the journal remains the **v5.6
  evolution** (see [JOURNAL.md](../../features/JOURNAL.md)); the autonomous `%journal` directive lands with
  the thought-tools phase (v0.32).
