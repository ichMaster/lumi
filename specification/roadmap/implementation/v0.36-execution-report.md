# Version v0.36 — Execution Report

**Date:** 2026-06-22
**Branch:** v0.36-dev
**Label:** v0::version:0
**Target version:** 0.36.0
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 5 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Lean memory III — the **facts** tier. Facts now reach the prompt **three ways** (mirroring messages):
the always-injected **core** (static), an **auto fact-RAG** block (per-turn push), and the
**`recall(scope=facts)`** tool (on-demand pull) — plus a **hygiene** path (`obsolete` flag + a
`/review-facts` skill). Off by default throughout; full suite green (1156 tests).

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-141 | Fact embedding + recall(scope=facts) | v0.36 | completed | 20a3f19 | 6 | pass |
| 2 | LUMI-142 | The core flag + lifecycle | v0.36 | completed | 02aab91 | 8 | pass |
| 3 | LUMI-143 | Shrink the facts block (core-only) + reconstruction | v0.36 | completed | 372932a | 4 | pass |
| 4 | LUMI-144 | Auto fact-RAG (per-turn push) | v0.36 | completed | 62f1092 | 5 | pass |
| 5 | LUMI-145 | Facts hygiene — obsolete flag + /review-facts skill | v0.36 | completed | be9704b | 8 | pass |

## Detailed Results

### LUMI-141: Fact embedding + recall(scope=facts)

**Status:** completed · **Commit:** 20a3f19

- `VectorRecord.kind` discriminator (`"message"` default | `"fact"`) — additive, back-compatible.
- `fact_vector_id` (content-addressed, idempotent); `_embed_facts` / `_index_facts` embed each
  `LongTermFact` as a `kind="fact"` vector on write; `backfill_facts()` (from `ensure_backfill`) embeds
  existing facts once.
- `search_vectors(…, kind=…)` filter; `recall` / `recall_moments` / the recall tool gain
  `scope = messages | facts | all` (default `messages` → byte-identical; facts never leak into the message paths).

**Validation:** `tests/integration/test_fact_recall.py` (6) + `test_memory_records` updated; ruff pass; no paid calls.

### LUMI-142: The core flag + lifecycle

**Status:** completed · **Commit:** 02aab91

- `LongTermFact.core` (additive). Curated three ways: a one-off **backfill** (first run picks from all
  facts), an **initial guess at extraction** (the `[C]` marker → `parse_facts_with_core`), and a
  **session-start re-flag** (`_ensure_core_flags`) re-ranking the `core=true` pool to `LUMI_FACTS_CORE_MAX`
  with **boundaries pinned** (`is_pinned_fact`) — input is the small core pool, not all facts.
- `repo.set_fact_core`; `LUMI_FACTS_CORE_MAX` (0 = off).

**Validation:** `tests/integration/test_facts_core.py` (7); full suite 1135; ruff pass; no paid calls.

### LUMI-143: Shrink the facts block (core-only) + reconstruction

**Status:** completed · **Commit:** 372932a

- With `LUMI_FACTS_CORE_ONLY` on, the `## Факти` block injects only the `core=true` facts (safe fallback
  to digest/raw when none flagged); the session-start re-flag **replaces** the per-turn `_ensure_facts_digest`
  call (cost-neutral). `LUMI_RECALL_SCOPE` sets the recall tool's default scope.
- **Reconstruction test:** a dropped tail fact stays reachable via `recall(scope=facts)`.

**Validation:** `tests/integration/test_facts_core_block.py` (7); ruff pass; no paid calls.

### LUMI-144: Auto fact-RAG (per-turn push)

**Status:** completed · **Commit:** 62f1092

- `_fact_recall_block(query)` — top-`LUMI_FACTS_RAG_K` relevant **non-core** facts (`scope=facts`, above
  `rag_floor`), **deduped against the core block**; a `# Релевантні факти` block in the **volatile tail**
  (never cached). `build_system_prompt`/`_system_prompt` gain a `fact_recall` slot. `LUMI_FACTS_RAG`/`_K`.

**Validation:** `tests/integration/test_fact_rag.py` (7); ruff pass; no paid calls.

### LUMI-145: Facts hygiene — obsolete flag + /review-facts skill

**Status:** completed · **Commit:** be9704b

- `LongTermFact.obsolete` (additive). Excluded from **all three** fact paths (core block, auto fact-RAG,
  `recall(scope=facts)`) at the fact-search + core-selection points, and from the re-flag pool — kept in the
  store (non-destructive, reversible). `repo.set_fact_obsolete`.
- **`/review-facts` Claude Code skill** (`.claude/skills/review-facts/SKILL.md`): propose → human review →
  apply (dup/outdated/irrelevant, reasons); store-free; **never auto-obsoletes a `core` fact**.

**Validation:** `tests/integration/test_facts_obsolete.py` (7) + `test_memory_records` updated; full suite 1156; ruff pass; no paid calls.

## The three fact paths (+ hygiene)

| Path | Mechanism | Flag |
|------|-----------|------|
| **Core (static)** | always-injected `core=true` facts (the `## Факти` block) | `LUMI_FACTS_CORE_ONLY` (+ `LUMI_FACTS_CORE_MAX`) |
| **Auto fact-RAG (push)** | `# Релевантні факти` — top-K relevant non-core facts per turn | `LUMI_FACTS_RAG` (+ `LUMI_FACTS_RAG_K`) |
| **Recall (pull)** | `recall(scope=facts)` tool | `LUMI_RECALL_SCOPE` |
| **Hygiene** | `obsolete` flag excluded from all paths; `/review-facts` skill | (the skill; no env flag) |

## Validation summary

- **Tests:** 1156 passed (5 new integration files, 34 new tests; the memory-records contract test updated for `kind` + `core` + `obsolete`). Mock model + mock embedder — **no paid calls**.
- **Lint:** ruff clean across `core/`, `state/`, and the new tests.
- **Contracts:** `VectorRecord` (+`kind`) and `LongTermFact` (+`core`,`+obsolete`) are additive — old records load with the defaults; ARCHITECTURE updated in the same commits.
- **Isolation:** every fact path is per-user (contract-tested); facts never leak into the message paths.
- **Reversibility:** all four mechanisms are off by default and byte-identical when off.

## Next Steps

- v0.36 issues all complete — the phase is ready to **release** (`/release-version 0.36.0`) on the user's confirmation.
- The `/review-facts` skill is operator-run (offline); the `obsolete` filter is code-side and mock-tested.
- Next roadmap phases: v0.37 (lean memory IV — thoughts tier via the `scope` seam), v0.38 (scheduler), v0.39 (thematic).
