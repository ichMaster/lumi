# Version v0.42 — Execution Report

**Date:** 2026-07-05
**Branch:** v0.39-dev
**Label:** v0::version:0
**Phase:** v0.42 — Thought scheduler (proactive thoughts on a clock — an in-TUI module)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-166 | Trigger model — `due()` + `schedule.toml` parser + `schedule.state` + config | v0.42 | completed | 4d0b55c | 4 | pass (1361) |
| 2 | LUMI-167 | In-TUI scheduler loop — tick → `due()` → `run_directive`, catch-up, caps + operator guide | v0.42 | completed | 410b299 | 4 | pass (1371) |
| 3 | LUMI-168 | Tick service — fast timer for ephemeral code handlers (`%update_state`) | v0.42 | completed | d0b0f5b | 3 | pass (1374) |
| 4 | LUMI-169 | Migrate idle triggers → `idle:` entries; retire `_maybe_think`/`_maybe_nudge` | v0.42 | completed | ceb11f8 | 5 | pass (1378) |

## Detailed Results

### LUMI-166: Trigger model

**Status:** completed · **Commit:** 4d0b55c
**Files:** `core/schedule.py` (new), `core/schedule.toml` (new), `core/config.py`, `tests/unit/test_schedule.py` (new)

- `Trigger` + `ScheduleEntry` dataclasses and a pure **`due(now, last_fired, trigger, *, last_input)`**
  for all five triggers (every/idle/at/between/cron); `at`/`cron` guard once-per-minute, `between+every`
  never double-fires, a 5-field cron evaluator (`*`, `*/n`, `a-b`, `a,b`, dom/dow OR, Sun 0/7).
- The `schedule.toml` parser (`tomllib`) with **raw** `{placeholder}` topics; `schedule.state` last-fired
  read/write + content-stable entry id; the `LUMI_SCHED*` config vars + an authored `core/schedule.toml`.
- 15 tests.

**Validation:** ✅ pytest · ✅ ruff · ✅ acceptance criteria (all) · no paid calls, no real sleeps

### LUMI-167: In-TUI scheduler loop

**Status:** completed · **Commit:** 410b299
**Files:** `tui/scheduler.py` (new), `tui/app.py`, `docs/SCHEDULER_SETUP.md` (new), `tests/unit/test_scheduler.py` (new)

- `tui/scheduler.py` `Scheduler`: `due_now()` (evaluate `due()`, quiet-hours veto sparing explicit `at:`,
  per-day + per-directive caps, stamp) + `catch_up()` (most-recent missed fixed-time occurrence within the
  window; live triggers excluded).
- `LumiApp`: a `set_interval` tick runs due entries **serialized** off-thread via `Core.run_directive`
  (in-process `%`-router — no bus, no daemon); a startup catch-up pass; `idle:` reads in-memory
  `_last_activity`.
- `docs/SCHEDULER_SETUP.md` operator guide. 10 tests (incl. the `run_directive` → `Thought` seam).

**Validation:** ✅ pytest · ✅ ruff · ✅ acceptance criteria (all)

### LUMI-168: Tick service

**Status:** completed · **Commit:** d0b0f5b
**Files:** `tui/scheduler.py`, `tui/app.py`, `tests/unit/test_scheduler.py`

- `TickService`: a name→callback registry + `tick()` running each handler — **no `Thought`, no model
  call**; fire-and-forget (a raiser swallowed, a re-entrant tick dropped, a missed tick a no-op).
- `LumiApp` starts the fast timer when the scheduler is on; `register_tick(name, handler)` is the seam
  v1.1 wires `%update_state` into. 3 tests.

**Validation:** ✅ pytest · ✅ ruff · ✅ acceptance criteria (all)

### LUMI-169: Migrate idle triggers, retire in-app timers

**Status:** completed · **Commit:** ceb11f8
**Files:** `tui/scheduler.py`, `tui/app.py`, `core/schedule.toml`, `tests/unit/test_scheduler.py`, `tests/unit/test_schedule.py`

- The idle `%think` entry ships **enabled** (migrated default-on idle, still gated by `LUMI_THOUGHTS`);
  the legacy `_maybe_nudge`/`_maybe_think` `set_interval`s are gated off when the scheduler is on and
  **byte-identical when off**.
- `_run_scheduled` graduates an idle-muse fire (`%think`/`%wonder`) to a **spoken self-turn** a fraction
  of the time (`graduates()` reuses the deterministic `should_graduate`) — subsuming the v0.4 nudge.
- 5 tests (graduation, migrated schedule, two pilots asserting the wiring on/off).

**Validation:** ✅ pytest (full suite, **1378 passed**) · ✅ ruff · ✅ acceptance criteria (all)

## Next Steps

- All v0.42 issues are done; no open issues remain. Release `0.42.0` via `/release-version` when you
  decide — the version is **not** bumped automatically.
- The scheduler is **off by default** (`LUMI_SCHEDULER`); turn it on + set `enabled = true` on the
  `core/schedule.toml` rows you want (see `docs/SCHEDULER_SETUP.md`).
- The v0.42 tick service exposes `register_tick` — the seam **v1.1** uses for `%update_state` (the
  needs/inner-life advance-to-`now`).
