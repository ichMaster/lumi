# Version v0.40 — GitHub Issues Report

**Uploaded:** 2026-07-02
**Repository:** https://github.com/ichMaster/lumi
**Phase:** v0.40 — Model routing: per-operation tiers (cost control, off by default)
**Total issues:** 5

## Issue Mapping

| LUMI ID | GitHub # | Title | Phase | Labels | URL |
|---------|----------|-------|-------|--------|-----|
| LUMI-155 | #155 | Layer 1 — per-operation model routing (`_model_for(kind)` + tier config + provider guard) | v0.40 | v0::version:0, v0::size:M, v0::area:core | https://github.com/ichMaster/lumi/issues/155 |
| LUMI-156 | #156 | Reply-tier dial — pin `/model <tier>` composition with routing + MODELS_SETUP.md | v0.40 | v0::version:0, v0::size:S, v0::area:core, v0::area:tui | https://github.com/ichMaster/lumi/issues/156 |
| LUMI-157 | #157 | `read_file`/`read_around` char cap — the un-shipped result-size delta | v0.40 | v0::version:0, v0::size:S, v0::area:core | https://github.com/ichMaster/lumi/issues/157 |
| LUMI-158 | #158 | Layer 2 — per-step routing inside the Anthropic tool-loop (optional, gated, off) | v0.40 | v0::version:0, v0::size:M, v0::area:core | https://github.com/ichMaster/lumi/issues/158 |
| LUMI-159 | #159 | Cost observability — verify the routing landed + re-measure the saving per kind | v0.40 | v0::version:0, v0::size:S, v0::area:core | https://github.com/ichMaster/lumi/issues/159 |

## Dependencies

- **#155** (Layer 1 routing) is the **foundation** — independent; start first.
- **#157** (read char cap) is **independent** — runs in parallel with #155.
- **#156** (dial pinned) is **blocked by #155** — the composition test exercises the routing (comment posted).
- **#158** (Layer 2, gated) is **blocked by #155** — reuses the tier config pattern; Anthropic loop only (comment posted).
- **#159** (observability) is **blocked by #155 + #157** — the changes it measures; #158 optional, measured with its flag off unless the A/B is running (comment posted).

Build order: **(#155 ‖ #157) → (#156 ‖ #158) → #159**.

## Labels Created

None — all required labels already existed:
- v0::version:0
- v0::size:S, v0::size:M
- v0::area:core, v0::area:tui

## Notes

- **LUMI-155** = the headline: the `_model_for(kind)` resolver + `LUMI_MODEL_THINK`/`_MOOD`/`_HOUSEKEEPING` + **one line** in `_housekeeping_reply`, with the **post-v0.39 provider guard** (routing only while `_active_provider == "anthropic"` — no Claude id ever reaches a foreign API).
- **LUMI-156** = **no new command**: the dial is the shipped v0.37 `/model opus ⇄ sonnet ⇄ haiku` aliases; this issue only pins that the dial **composes** with routing (reply moves, routed ops keep their tiers) + the MODELS_SETUP.md section.
- **LUMI-157** = only the **un-shipped delta** of the read cap: `LUMI_FILE_READ_MAX_CHARS` on `read_file`/`read_around` with an explicit truncation marker (the line caps + `search_files` char cap shipped with v0.19/v0.32).
- **LUMI-158** = the gated two-pass "dig on Sonnet, speak on Opus" pattern, scoped to **`AnthropicClient._tool_loop` only**; off by default, A/B before enabling.
- **LUMI-159** = close the phase with **measured numbers** (the −26% target and ~29% tool-bill estimate predate the shipped caps); operator-run, recorded in `LLM_OPERATIONS_COST.md`.
- **Kind vocabulary is the implemented one** (`think` / `mood` / `session-start` / `session-close` / `compaction`; summaries + facts run under the `session-*` kinds).
- **No contract change**; **off (all vars unset) → one model, byte-identical** (pinned per issue); no paid calls in tests.
- Design docs: `docs/LLM_OPERATIONS_COST.md` (measured analysis), `docs/MODEL_ROUTING_IMPLEMENTATION.md` (code design).
