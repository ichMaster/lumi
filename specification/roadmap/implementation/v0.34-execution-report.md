# Version v0.34 — Execution Report

**Date:** 2026-06-22
**Branch:** v0.34-dev
**Label:** v0::version:0
**Target version:** 0.34.0 (not bumped — awaiting `/release-version`)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Phase **v0.34 — Lean memory: tool-pull, not push (day/week index + style trims)** complete. The day/week
digests can render as a one-line dated index (`LUMI_MEMORY_INDEX`), a `/regen-summaries` command applies that
format to existing data, and the style block is compressed — all reversible, off → byte-identical, no contract
change. Full suite **1100 → 1109**; ruff clean throughout.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-136 | Day/week digests → a dated one-line index | v0.34 | completed | ca94f8a | 7 | pass |
| 2 | LUMI-137 | Regenerate the stored day/week digests | v0.34 | completed | f2a2f0f | 4 | pass |
| 3 | LUMI-138 | Compress the style palette | v0.34 | completed | 7b96c4a | 4 | pass |

## Detailed Results

### LUMI-136: Day/week digests → a dated one-line index

**Status:** completed · **Commit:** ca94f8a
**Files changed:**
- `core/memory.py` — `DAY_GIST_SYSTEM`/`WEEK_GIST_SYSTEM` + `index=` kwarg on `day/week_summary_request`
- `core/config.py` — `memory_index` field + `LUMI_MEMORY_INDEX` parse (off by default)
- `core/agent.py` — `memory_index` threaded into `Core`/`build_core`; `ensure_day/week_summaries` clamp to 1 row in index mode
- `tests/unit/test_day_summary.py`, `test_week_summary.py`, `test_config.py` (added); `.env.example` (documented)

**Validation:** unit + config pass · full suite 1104 · ruff pass · off → byte-identical (paragraph prompts).
The row cap (`MAX_*_ROWS`) is *not* the lever — these digests are a single paragraph; the length lives in the
prompt. Window knobs `LUMI_DAY_DAYS`/`LUMI_WEEK_DAYS` already existed.

### LUMI-137: Regenerate the stored day/week digests

**Status:** completed · **Commit:** f2a2f0f
**Files changed:**
- `core/agent.py` — `regenerate_summaries()` + a `force=` flag on `ensure_day/week_summaries` (now returns the rebuilt count)
- `tui/app.py` — `/regen-summaries` command
- `tests/unit/test_day_summary.py` (3 tests); `.env.example` (points at the command)

**Validation:** unit pass (force rebuilds a count-matching day; lossless + idempotent + per-user; empty window
is a no-op) · full suite 1107 · ruff pass. Lossless because the digests derive from the kept session
summaries (source untouched).

### LUMI-138: Compress the style palette

**Status:** completed · **Commit:** 7b96c4a
**Files changed:**
- `core/prompt.py` — `STYLE_HEADER` 278→177 chars (anchors kept)
- `core/styles.md` — the 6 meta-style descriptions tightened (every form-limit retained)
- `core/agent.py` — dropped the redundant palette intro (`"Мега-стилі — обери один…"` → `"Палітра:"`)
- `tests/integration/test_styles.py` (2 tests)

**Validation:** 24 style tests pass · full suite 1109 · ruff pass. Rendered `# Стиль відповіді` block
**1027 → 758 chars (~26%)**. The block was already lean (~1KB) — the ~3.5KB seen in a dumped prompt was a
customised `styles.md` with more meta-styles, so 50% wasn't safe without dropping the form-limits; the header
+ per-description trims benefit any `styles.md`.

## Combined effect

With `LUMI_MEMORY_INDEX=on` + `/regen-summaries`, the day/week tiers collapse to a dated index (~−18% of the
prompt) and the style block is ~26% smaller — the low-risk slice of the lean-memory line. The riskier tiers
remain their own phases: conversations **v0.35**, facts **v0.36**, thoughts **v0.37**.

## Next Steps

- All v0.34 issues complete — the phase is ready for `/release-version 0.34.0` (not auto-bumped).
- Next phases (the rest of the lean-memory line): **v0.35** (gist the conversation tier), **v0.36** (facts
  core + `recall(scope=facts)`), **v0.37** (thoughts cap + thought-RAG).
