# Version v0 (phase v0.12) — Execution Report

**Date:** 2026-06-09
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.12 — Thought-stream
**Target version:** 0.12.0 (not bumped — awaiting confirmation)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 7 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Final suite: **415 passed** (from a 366 baseline — **+49 tests**), ruff clean across `core/ tui/
state/ tests/`. **No paid API calls** (mock model + fixed clock + injected seed; no real sleeps).
**No contract change** — the emotion channel is reused verbatim; the new `Thought` store is the
first **global** (not user-keyed) store, with its A→B isolation pinned by a contract test.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-046 | Global `Thought` store (dated diary) + isolation contract test | v0.12 | completed | ec759a6 | 3 | pass |
| 2 | LUMI-047 | Mental-act engine + registry + `%think` / `%wonder` | v0.12 | completed | b480eae | 4 | pass |
| 3 | LUMI-048 | `{name}` placeholder resolver (registry + isolation-aware) | v0.12 | completed | 98d329b | 3 | pass |
| 4 | LUMI-049 | Feedback block — the last-24h dated diary + mood nudge | v0.12 | completed | 6e9db83 | 6 | pass |
| 5 | LUMI-050 | Proactive nudge — silent `%think` + graduate-to-spoken | v0.12 | completed | 60508e1 | 4 | pass |
| 6 | LUMI-051 | Input router (`/`·`%`·chat) + `%`-grammar + access gate | v0.12 | completed | 1f95704 | 4 | pass |
| 7 | LUMI-052 | `/thoughts` command + `thoughts_show` policy | v0.12 | completed | ed25d1c | 4 | pass |

## Detailed Results

### LUMI-046 — Global `Thought` store (dated diary) + isolation contract test
**Status:** completed · **Commit:** ec759a6 · `core/repository.py`, `state/local_store.py`, `tests/unit/test_thought_store.py`
- `Thought{when, kind, text, emotion, seeds, user_id, spoken, ts}` + `make_thought`; `Repository` seam `add_thought` / `thoughts_since` (raw global) / `thoughts_for` (per-user surfacing) / `prune_thoughts` (age cap). Stored as a **flat global list**, not a dict-by-user.
- Validation: 6 unit/contract tests — dated read, window, **A→B isolation**, list-not-user-keyed, prune, round-trip.

### LUMI-047 — Mental-act engine + registry + `%think` / `%wonder`
**Status:** completed · **Commit:** b480eae · `core/thoughts.py`, `core/agent.py`, `core/config.py`, `tests/unit/test_thoughts_engine.py`
- `core/thoughts.py`: directive registry + `thought_request` (seeded by mood/closeness/recent/last-thoughts + topic + injected seed) + `parse_thought`. `Core.think` runs seed → thinking-off call → validate → record; best-effort.
- Validation: 9 tests. `LUMI_THOUGHTS` config.

### LUMI-048 — `{name}` placeholder resolver
**Status:** completed · **Commit:** 98d329b · `core/placeholders.py`, `core/agent.py`, `tests/unit/test_prompt_placeholders.py`
- `resolve_placeholders` (unknown → literal); `Core.resolve` + the registry from live state; `{last_thought}`/`{thoughts}` **isolation-aware**; `think` resolves `{placeholders}` in the topic.
- Validation: 6 tests.

### LUMI-049 — Feedback block — the last-24h dated diary + mood nudge
**Status:** completed · **Commit:** 6e9db83 · `core/thoughts.py`, `core/agent.py`, `core/prompt.py`, `core/mood.py`, `core/config.py`, `tests/unit/test_thoughts_feedback.py`
- `thoughts_diary_block` + `_thoughts_block`: a `# Що в мене на думці (за останню добу)` section (rolling `LUMI_THOUGHTS_WINDOW_H`, dated, capped, per-user, empty→none). Soft mood nudge via `mood_request(thoughts=…)`.
- Validation: 7 tests.

### LUMI-050 — Proactive nudge — silent `%think` + graduate-to-spoken
**Status:** completed · **Commit:** 60508e1 · `core/thoughts.py`, `core/agent.py`, `core/config.py`, `tests/unit/test_thoughts_nudge.py`
- `should_graduate` + `Core.tick_think`: reuse `should_nudge` + per-session cap (reset in `start_session`) + quiet hours; silent by default, a fraction graduate (`spoken` set). Config `LUMI_THOUGHTS_INTERVAL_S`/`_CAP`/`_SPOKEN_RATIO`.
- Validation: 7 tests.

### LUMI-051 — Input router (`/`·`%`·chat) + `%`-grammar + access gate
**Status:** completed · **Commit:** 1f95704 · `core/thoughts.py`, `core/agent.py`, `tui/app.py`, `tests/unit/test_thoughts_directive.py`
- `parse_directive` (`%<name>[!] [connector] [topic]`, unknown→chat) + `directive_mode` (silent-vs-shared gate); `Core.run_directive` → `DirectiveOutcome`. TUI routes `%`-input: open → `💭` raw thought, silent → nothing, unknown → chat.
- Validation: 9 tests.

### LUMI-052 — `/thoughts` command + `thoughts_show` policy
**Status:** completed · **Commit:** ed25d1c · `core/agent.py`, `core/config.py`, `tui/app.py`, `tests/unit/test_thoughts_view.py`
- `Core.thoughts_view` (7-day dated, per-user) + `thoughts_show` (hidden/admin/off); each thought **logged** to `lumi.thoughts`, **never** persisted to long-term memory. TUI `/thoughts`.
- Validation: 5 tests.

## Phase DoD

Met ([ROADMAP.md §v0.12](../../ROADMAP.md)): on the nudge timer (paced) Лілі **`%think`s** from her
live state into a **global dated diary**, **mostly silently**; her **last-24h** dated thoughts
**feed back** into the next reply and softly nudge the mood; a fraction **graduate** to a spoken
turn; a typed **`%think[!] [topic]`** (with optional `{placeholders}`) fires it manually (`!` →
open, shown as `💭`); **`/thoughts`** shows the stream; a thought from user A **never** surfaces to
user B (contract test); the raw stream is **logged, never written to long-term memory**. **No
emotion-contract change. Never competence; honest about nature; restraint.** All tests against the
mock model + fixed clock + injected seed — no real sleeps, no paid calls.

## Next Steps

- All v0.12 issues complete; `v0.12-dev` pushed. **Version not bumped** — cut with
  `/release-version 0.12.0` when ready (and merge `v0.12-dev` → `main`).
- **Authoring follow-up (no code):** the `%think`/`%wonder` prompts + `{placeholder}` registry are
  in place; `core/inner_voice.md` (v0.15) will later share the same placeholder seam.
- Next phase: **v0.13 — Inner life I: plans & state** (with **needs**), which enriches the
  thought-stream's seed (`{plan}`/`{need}` resolve once it lands).
