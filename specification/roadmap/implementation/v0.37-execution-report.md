# Version v0.37 — Execution Report

**Date:** 2026-06-23
**Branch:** v0.37-dev
**Label:** v0::version:0
**Target version:** 0.37.0
**Phase:** v0.37 — OpenAI engine: tool-loop + runtime model toggle (GPT-5.5 ↔ Opus 4.8)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All three v0.37 issues are implemented, validated (mock transports — **no paid API calls**), committed
one-per-issue, pushed, and closed on GitHub. The full suite stays green (1188 passing). The Anthropic path
and the `{reply, emotion, intensity}` contract are untouched; off → byte-identical.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-146 | OpenAI tool-loop (function-calling port) | v0.37 | completed | 2557fef | 3 | pass |
| 2 | LUMI-147 | reasoning_effort passthrough (LUMI_EFFORT → OpenAI/DeepSeek) | v0.37 | completed | 955764b | 4 | pass |
| 3 | LUMI-148 | /model runtime engine toggle (Opus 4.8 ↔ GPT-5.5) | v0.37 | completed | 6c4d390 | 5 | pass |

## Detailed Results

### LUMI-146: OpenAI tool-loop (function-calling port)

**Status:** completed
**Commit:** 2557fef
**Files changed:**
- `core/llm.py` (modified) — `_openai_image`/`_openai_content`/`_openai_messages` translators; on
  `OpenAICompatibleClient`: `_to_openai_tools`, `_tool_loop` (JSON terminal), `_text_tool_loop` (think-path
  text terminal), `_run_tool_round`/`_assistant_tool_turn`/`_tool_result_msg` (untrusted + recollection
  framing, image divergence), `_round`/`_round_stats`/`_accumulate`/`_finalize_loop` (per-round log + summed
  `last_stats`), `last_round_log` init, `reply`/`reply_structured` wired to the loops.
- `tests/unit/test_openai_tool_loop.py` (added) — 15 cases, scripted `chat.completions` fake.
- `docs/MODELS_SETUP.md` (modified) — tools now work on OpenAI / DeepSeek / local; MiniMax still ignores them.

**Validation:**
- [x] Unit + contract tests: pass (49 LLM-focused; 1169 full at commit time)
- [x] Lint (ruff): pass
- [x] py_compile: pass
- [x] Acceptance criteria: all pass (tool execution + untrusted/recollection framing; terminal →
      `EmotionState`; forced final round always terminates; parallel calls; image divergence; no-tools
      byte-identical; Anthropic unchanged; no paid calls)

---

### LUMI-147: reasoning_effort passthrough (LUMI_EFFORT → OpenAI/DeepSeek)

**Status:** completed
**Commit:** 955764b
**Files changed:**
- `core/llm.py` (modified) — `_OPENAI_EFFORT` map (low/medium/high passthrough; xhigh/max → high; unknown →
  omitted); `OpenAICompatibleClient.__init__(effort=…)`; `_apply_effort` applied in `_create` + the
  tool-loop rounds; `build_llm` threads `cfg.effort`; the degradation log split (effort honored on the
  OpenAI-compatible adapter, still Anthropic-only thinking; MiniMax still ignores effort).
- `tests/unit/test_llm_openai.py` (modified) — effort set/unset/clamp/invalid-dropped + plain `reply` +
  `build_llm` threading (importorskip openai).
- `tests/unit/test_openai_tool_loop.py` (modified) — effort rides every loop round.
- `docs/MODELS_SETUP.md` (modified) — `LUMI_EFFORT` now honored on OpenAI/DeepSeek.

**Validation:**
- [x] Unit + contract tests: pass (47 focused; 1176 full at commit time)
- [x] Lint (ruff): pass
- [x] py_compile: pass
- [x] Acceptance criteria: all pass (`reasoning_effort` present when set / absent when unset; xhigh/max →
      high; invalid dropped safely; Anthropic unchanged; no paid calls)

---

### LUMI-148: /model runtime engine toggle (Opus 4.8 ↔ GPT-5.5)

**Status:** completed
**Commit:** 6c4d390
**Files changed:**
- `core/agent.py` (modified) — `Core.switch_model(provider, model)` (rebuild via injected factory +
  re-point; old client kept on failure); `Core.resolve_model_target` (alias / `provider:model`, clear
  reject); `provider` + `model_aliases` properties; `build_core` wires the factory closure (→ `build_llm`)
  + `cfg.model_aliases` + `cfg.provider`; `LLMError` import.
- `core/config.py` (modified) — `DEFAULT_MODEL_ALIASES`, `_parse_model_aliases` (`LUMI_MODEL_ALIASES`),
  `Config.model_aliases` field.
- `tui/app.py` (modified) — `/model` command (show / swap, status-bar refresh, clear error paths); help
  subtitle + `LLMError` import.
- `tests/unit/test_model_switch.py` (added) — 12 cases.
- `docs/MODELS_SETUP.md` (modified) — runtime `/model` toggle section.

**Validation:**
- [x] Unit + contract tests: pass (12 focused; 1188 full suite)
- [x] Lint (ruff): pass
- [x] py_compile: pass
- [x] Acceptance criteria: all pass (no-arg prints engine + aliases; swap without restart; status bar
      updates; rebuild from config keys; contract + isolation untouched; aliases from config; unknown
      rejected; no paid calls)

## Validation summary

- **Full test suite:** `uv run pytest` → **1188 passed** (no paid APIs — OpenAI transport, model, and the
  switch factory are all mocked).
- **Lint:** `ruff check` clean on every changed path.
- **Contracts:** the emotion field `{reply, emotion, intensity}`, memory record shapes, and per-user
  isolation are unchanged — no contract-test edits were required (the v0.3 gate already covers JSON-mode /
  structured output across providers). The Anthropic path is byte-identical.

## Next Steps

- **Release:** version not bumped (per policy — awaiting explicit confirmation). When confirmed, roadmap
  phase `v0.37` → semver **0.37.0** via `/release-version 0.37.0`.
- **Live verify (optional, not in CI):** with `LUMI_PROVIDER=openai` + `LUMI_MODEL=gpt-5.5`, send a turn
  that needs a file/wiki tool and confirm the tool fires and `.lumi/cache-log.jsonl` tags `model=gpt-5.5`.
- **Composes with v0.38 (model routing):** `/model` sets the engine; v0.38 sets which tiers run the cheap
  ops — they stack.
