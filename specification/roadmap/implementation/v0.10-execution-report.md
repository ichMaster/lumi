# Version v0 (phase v0.10) — Execution Report

**Date:** 2026-06-09
**Branch:** main
**Label:** v0::version:0
**Phase:** v0.10 — Closeness (relationship level)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 5 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-037 | `Closeness` record + per-user persistence | v0.10 | completed | 432876d | 4 | pass |
| 2 | LUMI-038 | Relational read — per-turn `relation` (additive to emotion) | v0.10 | completed | ed011a6 | 5 | pass |
| 3 | LUMI-039 | Closeness engine — delta + 5-level bucketing + inertia + time decay | v0.10 | completed | 1a3e8be | 3 | pass |
| 4 | LUMI-040 | Authored levels (`core/closeness.md`) + prompt block + guardrail | v0.10 | completed | e1030b7 | 6 | pass |
| 5 | LUMI-041 | Surface — `/closeness` command (level by name) | v0.10 | completed | 971de85 | 3 | pass |

Final suite: **333 passed**, ruff clean. No paid API calls (model mocked throughout).

## Detailed Results

### LUMI-037: Closeness record + per-user persistence

**Status:** completed · **Commit:** 432876d

- `Closeness{user_id, value, level, last_ts}` per-user record + `Repository.get_closeness`/
  `set_closeness` (upsert) + a `closeness` JSON store section; `clear_memory` drops it.
- **Validation:** contract test (shape + per-user isolation) + persistence/reload/upsert/clear.

### LUMI-038: Relational read — per-turn `relation` field (additive to emotion)

**Status:** completed · **Commit:** ed011a6

- `core/closeness.py`: `RelationRead{warmth, vulnerability, playful, harm, manipulation}` +
  `validate_relation` (clamp [0,1]; missing/garbage → 0.0; never raises). The `_EMOTION_TOOL`
  schema gains an **optional** `relation` object; `required` stays `[reply, emotion, intensity]`.
  `RELATION_INSTRUCTION` asks for it; the reply turn parses `raw["relation"]` → `last_relation`.
- **Validation:** validate (clamp/missing/garbage/non-dict); schema contract (emotion unchanged +
  relation additive); reply-turn populates `last_relation` with emotion intact.

### LUMI-039: Closeness engine — delta + 5-level bucketing + inertia + time decay

**Status:** completed · **Commit:** 1a3e8be

- `update_closeness(current, read, now, user_id)`: exp **decay** toward `BASELINE` over days of
  silence (injected clock + `last_ts`); weighted **delta** (`+warmth/vulnerability/playful −
  harm/manipulation`); clamp to [0,100]; **re-bucket** into 5 levels with `INERTIA` (hysteresis).
  A fresh user starts at the baseline. The reply turn advances + persists it each turn.
- **Validation:** delta, inertia (no flap / promote-demote thresholds), time decay (fixed clock),
  baseline, reply-turn persists; per-user isolated.

### LUMI-040: Authored levels (`core/closeness.md`) + prompt block + guardrail

**Status:** completed · **Commit:** e1030b7

- Editable `core/closeness.md` — 5 levels (Ввічлива → Найрідніша) + behavior directives;
  `load_levels` parses it; `closeness_block` injects `CLOSENESS_HEADER` + the active behavior (like
  mood/style). **Guardrail** in the header + every level: shapes warmth/openness only, **never
  competence**. `LUMI_CLOSENESS_PATH` config; build_core loads it.
- **Validation:** levels parse (5 named); block has header+behavior+guardrail; **low-closeness turn
  still answers fully**; active-level block in the prompt.

### LUMI-041: Surface — `/closeness` command (level by name)

**Status:** completed · **Commit:** 971de85

- `Core.closeness_status() -> (level, name)`; TUI `/closeness` → "Близькість: <name> (рівень N з 5)"
  — name only, raw scores internal; footer updated.
- **Validation:** defaults when no record; reads the record by name; returns only (level, name).

## Phase DoD

Met ([ROADMAP.md §v0.10](../../ROADMAP.md)): the same user gets **warmer/more open** behavior as
warmth/vulnerability/playfulness accrue and **cooler/more reserved — but never unhelpful** — on
harm/manipulation; the level is **stable within a session** (inertia), **decays over days of
silence** and rebuilds with contact; **per-user, never crossing users**; `/closeness` shows the
level by name. The emotion contract is **untouched** (relation is additive); **competence is never
affected** (pinned by a contract-style test). All tests against the **mock model** (no paid calls).

## Next Steps

- All v0.10 issues complete. **Version not bumped** (awaiting confirmation) — cut with
  `/release-version 0.10.0` when ready.
- Next phase: **v0.11 — Face variants & mood themes**.
