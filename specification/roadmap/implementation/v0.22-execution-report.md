# Version v0.22 — Execution Report

**Date:** 2026-06-16
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.22 — Local image tool I: vision (see & describe)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All four v0.22 issues implemented, validated (model + vision mocked — no paid calls), committed, pushed,
and closed in dependency order. The full suite grew from 752 → **768 tests** (+16). The emotion-channel
contract and per-user isolation invariant are unchanged; no SDK leaked into `core`.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-091 | Extend the LLMClient seam for image content blocks (multimodal) + MockLLMClient | v0.22 | completed | a46eb8c | 3 | pass |
| 2 | LUMI-092 | view_image tool — load a sandbox image as a multimodal block + wiring + config | v0.22 | completed | 2c7d59e | 5 | pass |
| 3 | LUMI-093 | Shared-image input in the TUI (/image) + Core.reply hook + docs | v0.22 | completed | 8d791d8 | 5 | pass |
| 4 | LUMI-094 | Contract + untrusted-image + isolation + vision-cap + emotion tests | v0.22 | completed | c11a4f4 | 1 | pass |

## Detailed Results

### LUMI-091: LLMClient image-block seam

**Status:** completed · **Commit:** a46eb8c
**Files changed:**
- `core/images.py` (new) — `image_block(path|bytes, media_type?)` (provider-neutral, base64) +
  `is_image_block` / `images_in_messages`.
- `core/llm.py` — a message `content` may be a string OR a list of text/image blocks; a `tool_result`
  may carry an image; `AnthropicClient` translates neutral blocks → multimodal `source` form (messages +
  tool_results); `_tool_loop` builds an image tool_result; `MockLLMClient.images_seen`. No SDK in core.
- `tests/unit/test_images.py` — 7 unit tests.

**Validation:** unit 7 pass · tool-loop regression green · ruff/compile pass · full suite 752 pass.

---

### LUMI-092: view_image tool

**Status:** completed · **Commit:** 2c7d59e
**Files changed:**
- `core/imagetool.py` (new) — `ImageTools.view_image` over the shared `safe_path` guard (extracted from
  `FileTools._safe`); image-type + size check; returns an image block or error string.
- `core/files.py` — `safe_path` extracted (one sandbox guard for files + images).
- `core/config.py` — `LUMI_IMAGE` / `LUMI_VISION_MAX` / `LUMI_IMAGE_MAX_BYTES`.
- `core/agent.py` — `_image_tool_args` merged into `_turn_tools` behind `LUMI_IMAGE`; per-turn vision cap
  (counts successful loads); `_tool_trace_repr` renders image results as a marker.
- `tests/integration/test_image_tool.py` — 5 integration tests.

**Validation:** integration 5 pass · file/wiki/trace regression green · ruff/compile pass · suite 757 pass.

---

### LUMI-093: shared-image input (TUI /image)

**Status:** completed · **Commit:** 8d791d8
**Files changed:**
- `core/agent.py` — `Core.reply(..., *, images=None)` attaches image blocks to the user message (gated +
  capped); ephemeral (text persisted, image not).
- `tui/app.py` — `/image <path> [message]` reads the file, builds the block, runs the turn (marker +
  reply); gated, friendly notices.
- `.env.example` + `docs/IMAGE_SETUP.md`.
- `tests/integration/test_image_input.py` — 5 tests (core hook + Textual pilot).

**Validation:** integration 5 pass · TUI regression green · ruff/compile pass · suite 762 pass.

---

### LUMI-094: contract + safety tests

**Status:** completed · **Commit:** c11a4f4
**Files changed:**
- `tests/contract/test_image_contract.py` — 6 tests: untrusted view_image (real AnthropicClient loop) +
  emotion unchanged + provider source form; shared image = user data block; per-user isolation; vision
  cap; off → no vision; the emotion contract with vision active.

**Validation:** contract 6 pass · ruff (core/+tui/+tests/) pass · full suite **768 pass** — no paid/vision.

## DoD check (ROADMAP §v0.22)

- [x] With the flag on, a shared image yields a description in chat, and `view_image` on a sandbox file
      lets her describe it.
- [x] An image is treated as **untrusted** (embedded text is not obeyed; framed untrusted on view_image).
- [x] The per-turn vision cap holds; paths are sandboxed + per-user.
- [x] Off (default) → no vision attached, no tool offered.
- [x] The `{reply, emotion, intensity}` contract passes verbatim; no SDK in `core`; vision mocked.

## Next Steps

The v0.22 phase is complete (4/4 issues). The version has **not** been bumped — cut the release with
`/release-version 0.22.0` when ready. The next roadmap phase is **v0.23 — Local image tool II:
generation (text → PNG)** (`specification/features/IMAGE_TOOL.md` §v0.23), which builds on this seam.
