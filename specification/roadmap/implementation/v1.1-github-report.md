# Version v1.1 — GitHub Issues Report

**Uploaded:** 2026-07-12
**Repository:** https://github.com/ichMaster/lumi
**Phase:** v1.1 — Conversation moves: the anti-mirror engine (retrospective + arbiter in the think-phase)
**Total issues:** 5

## Issue Mapping

| LUMI ID | GitHub # | Title | Phase | Labels | URL |
|---------|----------|-------|-------|--------|-----|
| LUMI-175 | #175 | The `move` field — `set_state` (per provider) + the `Message` record + config | v1.1 | v1::version:1, v1::size:M, v1::area:core, v1::area:state | https://github.com/ichMaster/lumi/issues/175 |
| LUMI-176 | #176 | History with declared types — recent messages enter the prompt with their `move` | v1.1 | v1::version:1, v1::size:S, v1::area:core | https://github.com/ichMaster/lumi/issues/176 |
| LUMI-177 | #177 | Arbiter dynamics in code — data-visible bans/signals substituted via the resolver | v1.1 | v1::version:1, v1::size:M, v1::area:core | https://github.com/ichMaster/lumi/issues/177 |
| LUMI-178 | #178 | `inner_voice.md` v2 + canon audit — retrospective / typed voices / arbiter | v1.1 | v1::version:1, v1::size:L, v1::area:core | https://github.com/ichMaster/lumi/issues/178 |
| LUMI-179 | #179 | Integration & DoD — the rule-walk conversation, mirror-streak criterion, byte-identical off | v1.1 | v1::version:1, v1::size:M, v1::area:tests | https://github.com/ichMaster/lumi/issues/179 |

## Dependencies

- **#175** (the `move` field + `Message` record + `LUMI_MOVES`) is the **data foundation** — start first.
- **#176** (history with declared types) is **blocked by #175** — comment posted.
- **#177** (arbiter dynamics in code) is **blocked by #175 + #176** — comment posted.
- **#178** (`inner_voice.md` v2 + canon audit) is **blocked by #176 + #177** (the authoring half can start in parallel with #177) — comment posted.
- **#179** (integration + DoD sweep) is **blocked by #175..#178** — comment posted.

Build order: **#175 → #176 → (#177 ‖ #178-authoring) → #178 → #179**.

## Labels Created

- v1::version:1 (Version v1 — Personality)
- v1::size:S, v1::size:M, v1::size:L
- v1::area:core, v1::area:state, v1::area:tests

## Notes

- **LUMI-175** = the data: the additive `move` enum on `set_state` per provider (English values
  `deepen/position/object/develop/associate/example/return`, garbled → dropped), the optional
  `Message.move` column in store.json (None default, old stores load unchanged), `LUMI_MOVES` (off).
- **LUMI-176** = the validation input: recent Лілі lines enter the prompt with their declared type from
  the field — **no inline tags in the message text**, never rendered to the user.
- **LUMI-177** = the code half of the arbiter: a pure function over the tagged window → resolver-substituted
  hard bans (same type declared twice) + the topic-died hint; execution judgment stays with the model.
- **LUMI-178** = the headline authoring: canon audit (behavioral traits, the prominent anti-mirror rule,
  few-shot pairs) + the v2 think instruction (retrospective validation → typed voices → arbiter with the
  full rule table + emergency mode), selected only when `LUMI_MOVES` is on.
- **LUMI-179** = the DoD sweep: the scripted 20-exchange rule-walk, store assertions, no-leak checks,
  byte-identical off.
- **No contract change** — `{reply, emotion, intensity}` + per-user isolation untouched; **off
  (`LUMI_MOVES`) → byte-identical** (pinned per issue); **never competence**; the monologue stays
  logged-never-persisted; no paid calls in tests.
- Design docs: `specification/features/lili-conversation-system-concept-en.md` (+ the reality-anchored
  UA original `specification/features/ukrainian/lili-conversation-system-concept.md`); the roadmap entry
  is ROADMAP.md §v1.1.
