# Version v0.32 — GitHub Issues Report

**Uploaded:** 2026-06-20
**Repository:** https://github.com/ichMaster/lumi
**Phase:** v0.32 — File tool IV: search across files, by date, and by line context
**Total issues:** 3 (LUMI-123…125)

## Issue Mapping

| LUMI ID | GitHub # | Title | Phase | Labels | URL |
|---------|----------|-------|-------|--------|-----|
| LUMI-123 | #123 | search_files — full-text search across the sandbox (+ LUMI_FILE_SEARCH_*) | v0.32 | v0::version:0, v0::size:M, v0::area:core | https://github.com/ichMaster/lumi/issues/123 |
| LUMI-124 | #124 | read_around(path, line, k) — line-context read + the line-number chain | v0.32 | v0::version:0, v0::size:S, v0::area:core | https://github.com/ichMaster/lumi/issues/124 |
| LUMI-125 | #125 | list_files after/before date filter + the search_files→read_around chain + docs | v0.32 | v0::version:0, v0::size:M, v0::area:core | https://github.com/ichMaster/lumi/issues/125 |

## Dependencies

```
LUMI-123 (#123, search_files)  ─┐
LUMI-124 (#124, read_around)   ─┤
                                └─► LUMI-125 (#125)  — blocked by #123, #124
```

- **#123** and **#124** are independent (each a self-contained read-only tool on the shipped v0.19 loop;
  `read_around` chains from the shipped `find_in_file`), so they can be built in parallel.
- **#125** is blocked by #123 and #124 — it lands the third tool (the `list_files` date filter) and
  consolidates the cross-tool `search_files → read_around` chain test + the documentation.

## Labels Used

All pre-existing (no new labels created):
- v0::version:0
- v0::size:S, v0::size:M
- v0::area:core

## Next step

Implement in dependency order with **`/execute-issues v0::version:0`** (or per-issue, e.g.
`/execute-issues v0::version:0 --issue LUMI-123`). Tests ship with each issue; the model is mocked — no
paid calls.
