# Version v0.40 — Execution Report

**Date:** 2026-07-02
**Branch:** v0.39-dev
**Label:** v0::version:0
**Phase:** v0.40 — Model routing: per-operation tiers (cost control, off by default)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining (operator-run) | 1 |

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-155 | Layer 1 — per-operation model routing (`_model_for` + tiers + provider guard) | v0.40 | completed | 806da90 | 3 | pass (1306) |
| 2 | LUMI-157 | `read_file`/`read_around` char cap (`LUMI_FILE_READ_MAX_CHARS`) | v0.40 | completed | 303c73e | 4 | pass (1310) |
| 3 | LUMI-156 | Reply-tier dial — `/model <tier>` composition pin + MODELS_SETUP.md | v0.40 | completed | a9f013e | 2 | pass (1312) |
| 4 | LUMI-158 | Layer 2 — per-step routing in the Anthropic tool-loop (R2, gated) | v0.40 | completed | c03e83e | 4 | pass (1317) |
| 5 | LUMI-159 | Cost observability — verify + re-measure | v0.40 | **open (operator-run)** | — | — | — |

A pre-existing baseline failure was fixed first (commit 048f1a3): the `test_nudge.py` seeds test
still pinned `%think`/`%wonder` while the refreshed curiosity-mode seeds legitimately use the
v0.33 tool-thought directives — it now validates against the directive REGISTRY.

## Detailed Results

### LUMI-155: Layer 1 — per-operation model routing

**Status:** completed · **Commit:** 806da90
**Files changed:** `core/config.py`, `core/agent.py`, `tests/unit/test_model_routing.py` (new)

- `_model_for(kind)` on `Core`: `think` → `LUMI_MODEL_THINK`, `mood` → `LUMI_MODEL_MOOD`,
  `session-start`/`session-close`/`compaction`/bare `housekeeping` → `LUMI_MODEL_HOUSEKEEPING`;
  unset → `self._model`. One line changed in `_housekeeping_reply`; the reply path untouched.
- **Provider guard:** routing only while `_active_provider == "anthropic"` — a Claude tier id never
  reaches a foreign (gpt-5.5/gemini) client.
- 9 tests: kind mapping, per-call model recording (turn/think/mood/close), the tool-loop follows the
  routed op, unset → byte-identical, the provider guard (both directions), config parsing.

**Validation:** ✅ pytest (full suite) · ✅ ruff · ✅ acceptance criteria (all)

### LUMI-157: `read_file`/`read_around` char cap

**Status:** completed · **Commit:** 303c73e
**Files changed:** `core/files.py`, `core/agent.py`, `core/config.py`, `tests/unit/test_file_bounds.py`

- `FileTools._clamp_chars`: a result over `LUMI_FILE_READ_MAX_CHARS` (default 8000) is truncated with
  the explicit marker `… (capped at N chars; read a narrower range)` — never silent. Applied to
  `read_file` + `read_around` after the existing line clamps; the line caps and per-turn budget hold;
  `list_files`/`find_in_file`/`search_files` unchanged (already bounded).
- 4 tests: truncation + marker on both tools, ordinary files unmarked, composition with the line caps.

**Validation:** ✅ pytest (full suite) · ✅ ruff · ✅ acceptance criteria (all)

### LUMI-156: Reply-tier dial composition pin + docs

**Status:** completed · **Commit:** a9f013e
**Files changed:** `tests/unit/test_model_routing.py`, `docs/MODELS_SETUP.md`

- Two tests pin the composition: `/model haiku` (the shipped v0.37 aliases via `switch_model`) moves
  the reply while routed ops keep their tiers; with tier vars unset the dial moves everything (the
  v0.37 behaviour, unchanged). **No new command** — `/mode` dropped per the revised phase spec.
- `MODELS_SETUP.md` §1 gains "The three-tier dial + per-operation routing (v0.40)".

**Validation:** ✅ pytest (full suite) · ✅ ruff · ✅ acceptance criteria (all)

### LUMI-158: Layer 2 — per-step routing (R2, gated, Anthropic-only)

**Status:** completed · **Commit:** c03e83e
**Files changed:** `core/llm.py`, `core/config.py`, `tests/unit/test_tool_loop.py`, `docs/MODEL_ROUTING_IMPLEMENTATION.md`

- `AnthropicClient(step_routing, step_model)` → in both loops, round 0 and the visible terminal stay
  on the call's model; continuations dig on `LUMI_MODEL_TOOL_STEP`; a cheap terminal is discarded and
  one clean final call speaks on the voice (R2). Per-round log tags each round's actual model.
  OpenAI/Gemini loops untouched. Off by default (`LUMI_TOOL_STEP_ROUTING`).
- 5 tests + the A/B protocol note in the design doc.

**Validation:** ✅ pytest (full suite, 1317) · ✅ ruff · ✅ acceptance criteria (all)

### LUMI-159: Cost observability

**Status:** open — **operator-run by design** (real-usage measurement). Readiness instructions posted
on the issue: enable the tier vars, run ~2 weeks, re-run the cost report over `.lumi/cache-log.jsonl`,
record the "v0.40 measured" note in `docs/LLM_OPERATIONS_COST.md`.

## Next Steps

- **LUMI-159** (#159): operator measurement once real usage accumulates with the tier vars on.
- **Release:** all v0.40 code issues are done; cut `0.40.0` via `/release-version` when you decide —
  the version is **not** bumped automatically. (LUMI-159 can stay open past the release; it writes
  docs, not code.)
