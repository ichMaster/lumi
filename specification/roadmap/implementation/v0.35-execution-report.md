# Version v0.35 — Execution Report

**Date:** 2026-06-22
**Branch:** v0.35-dev
**Label:** v0::version:0
**Target version:** 0.35.0 (not bumped — awaiting `/release-version`)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 2 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Phase **v0.35 — Lean memory II: gist the conversation tier** complete. The detailed-conversations tier (the
prompt's biggest block) can now keep only the last `LUMI_SESSION_DETAIL_N` sessions verbatim and render the
rest as a dated one-line gist index; auto-RAG + the by-date / recall pull tools keep the gisted detail
reachable, while the verbatim sessions are deduped so nothing is injected twice. Reversible, off →
byte-identical, no contract change. Full suite **1114 → 1117**; ruff clean.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-139 | Gist the conversation tier | v0.35 | completed | 2a1df69 | 6 | pass |
| 2 | LUMI-140 | Auto-RAG covers the gisted window + the reconstruction test | v0.35 | completed | ac9e231 | 2 | pass |

## Detailed Results

### LUMI-139: Gist the conversation tier

**Status:** completed · **Commit:** 2a1df69
**Files changed:**
- `core/agent.py` — `Core._system_prompt` splits the session window: last `N` verbatim `summary`, older as a dated `gist`
- `core/memory.py` — `SESSION_DETAIL_N` (default 0) + `session_gist(gist, summary)` (gist, or a one-line fallback)
- `core/config.py` — `LUMI_SESSION_DETAIL_N` threaded `Config` → `Core` → `build_core`
- `tests/integration/test_session_gist.py`, `tests/unit/test_config.py`; `.env.example`

**Validation:** unit + integration pass (off → all verbatim; N=1 → last verbatim + older dated gist; helper;
per-user isolation) · full suite 1114 · ruff pass · `N=0`/`N≥window` → byte-identical.

### LUMI-140: Auto-RAG covers the gisted window + the reconstruction test

**Status:** completed · **Commit:** ac9e231
**Files changed:**
- `core/agent.py` — `_conversation_dedup_ids()` (the verbatim last-N sessions' message ids) folded into the
  auto-RAG + recall-tool dedup; empty when gisting is off (byte-identical)
- `tests/integration/test_session_gist.py` (3 reconstruction tests)

**Validation:** the auto-RAG dedup already keyed off the *live window* (not the summary tier), so a gisted
session resurfaces by construction — the added piece is the no-double-injection dedup of the verbatim
sessions. Tests: push (auto-RAG) + pull (by-date) reconstruction; a verbatim session's line is deduped; the
default-config `test_rag_turn` is unaffected. Full suite 1117 · ruff pass.

## Combined effect

With `LUMI_SESSION_DETAIL_N` small, the conversation tier collapses to the live thread (verbatim) + a dated
gist index for the rest (~−20–26% of the prompt). Older detail is reachable via auto-RAG (push) and
`recall` / `messages_on` / `messages_between` (pull); verbatim sessions aren't re-surfaced. Pairs with v0.34's
day/week index + style trims. Remaining lean-memory phases: facts core + `recall(scope=facts)` = **v0.36**;
thoughts cap + thought-RAG = **v0.37**.

## Next Steps

- All v0.35 issues complete — ready for `/release-version 0.35.0` (not auto-bumped).
- Next: **v0.36** (facts core + fact recall), **v0.37** (thoughts cap + thought-RAG).
