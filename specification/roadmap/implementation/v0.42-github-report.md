# Version v0.42 — GitHub Issues Report

**Uploaded:** 2026-07-05
**Repository:** https://github.com/ichMaster/lumi
**Phase:** v0.42 — Thought scheduler (proactive thoughts on a clock — an in-TUI module)
**Total issues:** 4

## Issue Mapping

| LUMI ID | GitHub # | Title | Phase | Labels | URL |
|---------|----------|-------|-------|--------|-----|
| LUMI-166 | #166 | Trigger model — `due()` + `schedule.toml` parser + `schedule.state` + config | v0.42 | v0::version:0, v0::size:M, v0::area:core | https://github.com/ichMaster/lumi/issues/166 |
| LUMI-167 | #167 | In-TUI scheduler loop — tick → `due()` → `run_directive`, catch-up, caps + operator guide | v0.42 | v0::version:0, v0::size:L, v0::area:tui | https://github.com/ichMaster/lumi/issues/167 |
| LUMI-168 | #168 | Tick service — fast timer for ephemeral code handlers (`%update_state`) | v0.42 | v0::version:0, v0::size:S, v0::area:tui | https://github.com/ichMaster/lumi/issues/168 |
| LUMI-169 | #169 | Migrate idle triggers → `idle:` entries; retire `_maybe_think`/`_maybe_nudge` | v0.42 | v0::version:0, v0::size:M, v0::area:tui | https://github.com/ichMaster/lumi/issues/169 |

## Dependencies

- **#166** (trigger model) is the **pure foundation** (core, no TUI) — everything waits on it; start first.
- **#167** (scheduler loop) is **blocked by #166** — the loop drives the trigger model (comment posted).
- **#168** (tick service) is **blocked by #166** (the fast-tick config) — **independent of #167**, a separate fast timer (comment posted).
- **#169** (migrate idle triggers) is **blocked by #167** — folds the idle rule onto the scheduler loop, then retires the in-app timers (comment posted).

Build order: **#166 → (#167 ‖ #168) → #169**.

## Labels Created

None — all required labels already existed:
- v0::version:0
- v0::size:S, v0::size:M, v0::size:L
- v0::area:core, v0::area:tui

## Notes

- **LUMI-166** = the pure foundation: `due(now, last_fired, spec)` for the five triggers (every/idle/at/between/cron, fixed-clock tests, no sleeps), the `core/schedule.toml` parser (raw placeholder topics), `schedule.state` (last-fired per entry), and the `LUMI_SCHED*` config vars + an authored `schedule.toml` (all `enabled=false`).
- **LUMI-167** = the headline in-TUI loop: tick → `due()` → `run_directive` directly, startup catch-up (most-recent within the cap), quiet-hours veto + per-directive/global day caps, in-memory `_last_activity` for `idle:` (no bus files), the outbox surfacing, plus the operator guide.
- **LUMI-168** = the separate fast tick service for **ephemeral code handlers** (`%update_state` is a callback — no `Thought`, no model call); ships the registry seam that v1.1 wires into.
- **LUMI-169** = fold the v0.4 nudge + v0.12 `%think` idle triggers into `idle:` entries and retire `_maybe_think`/`_maybe_nudge` (gated so scheduler-off keeps the legacy behavior byte-identical).
- **No core change** — the scheduler is a **TUI module**; **no bus, no daemon** (only `schedule.state`); **off → byte-identical**; per-user isolation + the `{reply, emotion, intensity}` contract untouched; no paid calls, no real sleeps.
- Design docs: `specification/features/THOUGHT_SCHEDULER.md`; the operator guide (`docs/SCHEDULER_SETUP.md`) ships with LUMI-167.
