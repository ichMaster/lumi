# Version v0.20 — Execution Report

**Date:** 2026-06-16
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.20 — Local file tool II: writing (create & append)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All three v0.20 issues implemented, validated (model mocked — no paid calls), committed, pushed, and
closed in dependency order. The full suite grew from 699 → **719 tests** (+20). The emotion-channel
contract and the per-user isolation invariant are unchanged.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-085 | core/files.py — non-destructive write tools (create_file, append_file) + per-write size cap | v0.20 | completed | 35b64af | 3 | pass |
| 2 | LUMI-086 | Register the write tools in Core.reply + wire LUMI_FILE_WRITE_MAX through config + docs | v0.20 | completed | 421e98e | 6 | pass |
| 3 | LUMI-087 | Contract + per-user isolation + write-behaviour tests | v0.20 | completed | ddd2323 | 1 | pass |

## Detailed Results

### LUMI-085: core/files.py — non-destructive write tools + per-write size cap

**Status:** completed · **Commit:** 35b64af
**Files changed:**
- `core/files.py` — added `create_file` (new-only) + `append_file` (end-only) to the `FileTools`
  executor, `WRITE_TOOLS`/`WRITE_TOOL_NAMES` schemas, a `_content` validator + `write_max` ctor param;
  reuses the v0.19 `_safe` traversal guard; every failure degrades to an error string.
- `core/config.py` — `file_write_max` (`LUMI_FILE_WRITE_MAX`, default 64 KB).
- `tests/unit/test_files.py` — +10 write tests.

**Validation:**
- [x] Unit tests: 21 pass (11 read + 10 write) — create/append, refusals, size cap, sandbox escapes,
      no-destructive-tool, missing content.
- [x] ruff: pass · py_compile: pass · full suite: 709 pass.
- [x] Acceptance criteria: all pass.

---

### LUMI-086: Register the write tools in Core.reply + wire the cap + docs

**Status:** completed · **Commit:** 421e98e
**Files changed:**
- `core/agent.py` — `_file_tool_args` returns `READ_TOOLS + WRITE_TOOLS` and builds `FileTools` with
  `write_max`; `file_write_max` threaded through the `Core` ctor + `build_core`; the v0.19 trace wraps
  the write tools.
- `core/config.py` — (already added in LUMI-085).
- `.env.example` — documents `LUMI_FILE_WRITE_MAX` + the non-destructive boundary.
- `docs/FILE_TOOL_SETUP.md` — title, tool tables (read + write), safety, config reference, troubleshooting.
- `tests/integration/test_file_tool.py` — +3 (create→append turn, off→no tools, write bound to user).
- `tests/integration/test_tool_trace.py` — +1 (trace wraps the write tools).

**Validation:**
- [x] Integration + trace tests pass; off → no tools offered; write bound to the active user's root.
- [x] ruff: pass · py_compile: pass · full suite: 713 pass.
- [x] Acceptance criteria: all pass. No contract change.

---

### LUMI-087: Contract + per-user isolation + write-behaviour tests

**Status:** completed · **Commit:** ddd2323
**Files changed:**
- `tests/contract/test_file_tool_contract.py` — +6 write contract tests: per-user isolation over
  create/append, create-over-existing & append-to-missing refused (original intact), per-write size cap
  through a full Core turn, sandbox escapes denied over the write tools, the emotion contract holding
  with the write tools active.

**Validation:**
- [x] Contract tests: 11 pass (5 read + 6 write).
- [x] ruff (core/ + tests/): pass · full suite: 719 pass — zero paid API calls.
- [x] Acceptance criteria: all pass.

## DoD check (ROADMAP §v0.20)

- [x] With the flag on, a turn can create a new file and append to it, confined to the user's sandbox.
- [x] create-over-existing, append-to-missing, and oversize writes are refused with a clear error and
      the turn still completes.
- [x] No overwrite or delete path exists.
- [x] The emotion contract and per-user isolation are unchanged.

## Next Steps

The v0.20 phase is complete (3/3 issues). The version has **not** been bumped — cut the release with
`/release-version 0.20.0` when ready (post-release fixes bump `0.20.x`). The next roadmap phase is
**v0.21 — Local dictation (STT)** (`specification/features/DICTATION_LOCAL.md`).
