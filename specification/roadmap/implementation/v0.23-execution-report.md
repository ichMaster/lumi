# Version v0.23 — Execution Report

**Date:** 2026-06-17
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.23 — Local image tool II: generation (text → PNG)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All three v0.23 issues implemented, validated (model + image generation mocked — no paid calls),
committed, pushed, and closed in dependency order. The full suite grew from 777 → **791 tests** (+14).
The emotion-channel contract and per-user isolation invariant are unchanged; no SDK leaked into `core`.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-095 | core/imagegen.py — ImageGen seam (Gemini, injected) + generate_image (create-only) + config | v0.23 | completed | 701c77b | 3 | pass |
| 2 | LUMI-096 | Wire generate_image into Core.reply behind LUMI_IMAGE (cap, no-personal-data, display) + docs | v0.23 | completed | 0fc9a7f | 5 | pass |
| 3 | LUMI-097 | Contract + create-only + no-personal-data + cap + isolation + emotion tests | v0.23 | completed | 01d31c3 | 1 | pass |

## Detailed Results

### LUMI-095: ImageGen seam + generate_image tool

**Status:** completed · **Commit:** 701c77b
**Files changed:**
- `core/imagegen.py` (new) — the injected `ImageGen` seam (`generate(prompt, *, size) -> bytes`, default
  `gemini_image_gen` — the Gemini Nano Banana caller lifted from the faces skill, no SDK) + the
  `generate_image` tool (`ImageMaker`): saves a new PNG under the sandbox via the shared `safe_path`
  guard, **create-only**, default slug filename under `art/`; every failure → error string, never raises.
- `core/config.py` — `LUMI_IMAGE_PROVIDER` / `_MODEL` / `_SIZE` / `_MAX_GEN` / `_SHOW`.
- `tests/unit/test_imagegen.py` — 9 unit tests (stub ImageGen).

**Validation:** unit 9 pass · ruff/compile pass · full suite 777 pass.

---

### LUMI-096: wire into Core.reply + cap + display

**Status:** completed · **Commit:** 0fc9a7f
**Files changed:**
- `core/agent.py` — `_imagegen_tool_args` merged into `_turn_tools` behind `LUMI_IMAGE` (injected
  `image_gen`, None → real Gemini); per-turn `LUMI_IMAGE_MAX_GEN` cap (successful generations);
  prompt passthrough (no personal data); `_emit_image_display` writes the PNG path to `.lumi/image.txt`
  per `LUMI_IMAGE_SHOW` (best-effort); config threaded `build_core` → `Core`.
- `core/config.py` — (the gen flags, with LUMI-095).
- `.env.example` + `docs/IMAGE_SETUP.md` — the generation section.
- `tests/integration/test_imagegen_tool.py` — 6 tests (mock model + stub ImageGen).

**Validation:** integration 6 pass · image/file/wiki regression green · ruff/compile pass · suite 783 pass.
No contract change.

---

### LUMI-097: contract + safety tests

**Status:** completed · **Commit:** 01d31c3
**Files changed:**
- `tests/contract/test_imagegen_contract.py` — 8 tests: create-only; the prompt is exactly the model's
  request (no personal data); the per-turn cap; a generator error degrades; a traversal filename is
  refused; per-user isolation; off → no tool; the emotion contract holds with `generate_image` active.

**Validation:** contract 8 pass · ruff (core/+tui/+tests/) pass · full suite **791 pass** — no paid calls.

## DoD check (ROADMAP §v0.23)

- [x] With the flag on (+ `GEMINI_API_KEY`), a turn generates a PNG into `.lumi/files/<user>/`,
      non-destructive, displayed per `LUMI_IMAGE_SHOW`.
- [x] The outgoing prompt carries no personal data; a provider refusal/error degrades to an error string
      and the turn completes; the per-turn cap holds.
- [x] Off (default) → the tool is absent; the emotion contract is unchanged; no SDK in `core`; generation
      mocked (no paid calls).

## Next Steps

The v0.23 phase is complete (3/3 issues). The version has **not** been bumped — cut the release with
`/release-version 0.23.0` when ready. The next roadmap phase is **v0.24 — Local dictation (STT)**
(`specification/features/DICTATION_LOCAL.md`). The v5 creative layer (gallery, async generation +
proactive turns, music, co-creation, journal) is the MCP/server-era successor to this local image tool.
