# Version v0.43 — GitHub Issues Report

**Uploaded:** 2026-07-08
**Repository:** https://github.com/ichMaster/lumi
**Phase:** v0.43 — Model roles: register-routed replies (talking / thinking / emotional)
**Total issues:** 5

## Issue Mapping

| LUMI ID | GitHub # | Title | Phase | Labels | URL |
|---------|----------|-------|-------|--------|-----|
| LUMI-170 | #170 | Roles in the profiles — `ModelProfile` + `core/models.toml` + config | v0.43 | v0::version:0, v0::size:S, v0::area:core | https://github.com/ichMaster/lumi/issues/170 |
| LUMI-171 | #171 | Lexical stage — `core/roles.py` + `core/roles.md` → `talk\|think\|emotional\|unsure` | v0.43 | v0::version:0, v0::size:M, v0::area:core | https://github.com/ichMaster/lumi/issues/171 |
| LUMI-172 | #172 | Classifier call — one structured `{label, confidence}` on the `classifier` role | v0.43 | v0::version:0, v0::size:M, v0::area:core | https://github.com/ichMaster/lumi/issues/172 |
| LUMI-173 | #173 | Hybrid firing + stickiness — the reply-path router + the hold | v0.43 | v0::version:0, v0::size:L, v0::area:core | https://github.com/ichMaster/lumi/issues/173 |
| LUMI-174 | #174 | Surfacing — status-bar register + `/roles` + `/model` suspends routing | v0.43 | v0::version:0, v0::size:S, v0::area:core, v0::area:tui | https://github.com/ichMaster/lumi/issues/174 |

## Dependencies

- **#170** (roles in profiles + config) is the **data foundation** — everything waits on it; start first.
- **#171** (lexical stage) is **blocked by #170** (the `LUMI_ROLES_FILE` config) — comment posted.
- **#172** (classifier call) is **blocked by #170** (the classifier role) — **independent of #171**; comment posted.
- **#173** (hybrid firing + hold) is **blocked by #170 + #171 + #172** — the headline; ties them into the reply path; comment posted.
- **#174** (surfacing) is **blocked by #173** — surfaces the router + hold; comment posted.

Build order: **#170 → (#171 ‖ #172) → #173 → #174**.

## Labels Created

None — all required labels already existed:
- v0::version:0
- v0::size:S, v0::size:M, v0::size:L
- v0::area:core, v0::area:tui

## Notes

- **LUMI-170** = the data: `ModelProfile` gains `talking`/`thinking`/`emotional`/`classifier` (each defaults
  to `reply`), the anthropic role set in `core/models.toml`, and the `LUMI_MODEL_ROLES`/`_COOLDOWN`/
  `_CONFIDENCE`/`_FILE` config.
- **LUMI-171** = the pure lexical stage (`core/roles.py` + authored `core/roles.md`): closers/distress/
  thinking markers + length/code/multi-question heuristics + the hold → `talk|think|emotional|unsure`, no LLM.
- **LUMI-172** = the one structured classifier call → `{label, confidence}`, with the **no-personal-data**
  contract test and floor/failure → talk.
- **LUMI-173** = the headline: hybrid firing in `Core.reply` (clear → one call; unsure → talking ∥
  classifier, escalate + discard draft) + the register hold (stickiness, short-message bypass, re-target,
  revert); off → byte-identical.
- **LUMI-174** = surfacing: status-bar register, `/roles` command, `/model`-suspends-routing + docs.
- **No contract change**; **off (`LUMI_MODEL_ROLES`) → byte-identical**; **never competence** (routing reads
  the user's message, never her mood); **no personal data in the classifier prompt** (pinned); **never
  blocks a turn**; provider-homogeneous; no paid calls in tests.
- Design doc: `specification/features/MODEL_ROLES.md` (+ the Ukrainian `MODEL_ROLES.uk.md`).
