# Version v0.4 — Execution Report

**Date:** 2026-06-07
**Branch:** main
**Label:** v0::version:0
**Phase:** v0.4 — Ambient context (now, here, weather, news) (version v0 — TUI)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All 4 issues (LUMI-019…022) implemented in dependency order, each as one commit
with its tests, closing its GitHub issue. `main` stayed green throughout; the full
suite grew **166 → 189** tests, ruff clean. Mock model + **injected clock/HTTP** —
no paid, network, or sleep-based tests.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Tests |
|---|---------|-------|-------|--------|--------|-------|
| 1 | LUMI-019 | Time-legible prompts — per-message timestamps + dated summaries | v0.4 | completed | dc4b6d5 | pass |
| 2 | LUMI-020 | WorldContext provider — clock + config-gated weather/news/location | v0.4 | completed | fddbcea | pass |
| 3 | LUMI-021 | Ambient block in the system prompt + startup fetch/refresh | v0.4 | completed | 6bc1e2c | pass |
| 4 | LUMI-022 | Idle nudge — a hidden self-started turn | v0.4 | completed | 4da791a | pass |

## Detailed Results

### LUMI-019: Time-legible prompts
**Status:** completed · **Commit:** dc4b6d5
**Files:** `core/clock.py` (added), `core/agent.py`, + tests
- Injectable clock (`Clock`/`system_clock`/`fixed_clock`) + `format_stamp`/`format_date`.
- Each replayed message prefixed with its date-time (new user line uses the clock's now); each recalled `ShortSummary` dated in the system prompt; message/summary `ts` from the clock (deterministic).

### LUMI-020: WorldContext provider
**Status:** completed · **Commit:** fddbcea
**Files:** `core/worldcontext.py` (added), `core/config.py`, `.env.example`, tests
- `WorldContext{now, calendar, location, weather, news}` + `fetch_world_context(clock, …)`: now/calendar from the clock; weather (Open-Meteo), news (RSS, capped), location (static) via thin HTTP behind an injected `http_get`. Config-gated; any source off/erroring → `None`/`()`; `fetch` never raises.

### LUMI-021: Ambient block + fetch/refresh
**Status:** completed · **Commit:** 6bc1e2c
**Files:** `core/worldcontext.py`, `core/prompt.py`, `core/agent.py`, `tui/app.py`, tests
- `ambient_line(wc, clock)` (date-time recomputed per turn; sources from the snapshot; coloring tone, never competence). `build_system_prompt(ambient=…)` opt-in; `Core.set_world_context` + injection. TUI fetches off-thread at startup + on `/new`, gated by config; unconfigured → no block, no error.

### LUMI-022: Idle nudge
**Status:** completed · **Commit:** 4da791a
**Files:** `core/nudge.py` (added), `core/nudges.md` (added), `core/config.py`, `.env.example`, `tui/app.py`, tests
- `should_nudge` (pure, clock-driven, quiet-hours) + `load_nudges`; authored openers. TUI `_run_turn(hidden=)` runs the nudge as a hidden user turn — reply shown, the injected line never enters the chat/transcript/copy. Idle poll, rate-limited, config-gated, off by default.

## Validation (per issue)
- [x] Unit + integration tests: pass (clock formatters, WorldContext provider, ambient block, nudge logic + hidden turn)
- [x] Lint (ruff): pass
- [x] No network / no real sleeps in CI: HTTP behind an injected `http_get`; timing via the injected clock
- [x] Acceptance criteria: all pass against the v0.4 DoD

## DoD check (ROADMAP §v0.4)
> At startup Лілі has a WorldContext snapshot (degrading gracefully); the system prompt
> carries the ambient block; every message and every recalled summary is timestamped;
> nothing hangs when a source fails. With the idle nudge on, a long silence yields a reply
> and no visible user message; real input resets the timer; off by default.

Met: per-message timestamps + dated summaries land every turn; the ambient block is
injected when configured (graceful otherwise); the idle nudge runs a hidden turn so
Лілі speaks first. The injected clock + `http_get` keep it all deterministic.

## Next Steps
- v0.4 is complete and unreleased. Cut it with `/release-version 0.4.0` when ready.
- Next phase: **v0.5 — Emoji rendering** (`EmojiRenderer` maps emotion→emoji beside the reply).
