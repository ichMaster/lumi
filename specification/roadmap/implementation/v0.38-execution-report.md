# Version v0.38 — Execution Report

**Date:** 2026-06-24
**Branch:** v0.37-dev
**Label:** v0::version:0
**Target version:** 0.38.0
**Phase:** v0.38 — Inner Voice: the authored three-voice think-phase instruction
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 2 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Both v0.38 issues are implemented, validated (mock model — **no paid API calls**), committed
one-per-issue, pushed, and closed on GitHub. The full suite stays green (1226 passing). The
`{reply, emotion, intensity}` contract is untouched and the off-path is byte-identical to today.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-149 | Inner Voice — authored core/inner_voice.md + loader + LUMI_INNER_VOICE | v0.38 | completed | 9084612 | 6 | pass |
| 2 | LUMI-150 | LUMI_THINK_SHOW — think-show modes + the think-log tier | v0.38 | completed | ae27129 | 6 | pass |

## Detailed Results

### LUMI-149: Inner Voice — authored core/inner_voice.md + loader + LUMI_INNER_VOICE

**Status:** completed
**Commit:** 9084612
**Files changed:**
- `core/inner_voice.md` (added) — the authored three-voice think instruction (Імпульс / Тверезість /
  Стандарт), weighing mood + closeness, with the five invariants + the `<think>` wrap line.
- `core/prompt.py` (modified) — `load_inner_voice(path)` (graceful `None` on missing/empty).
- `core/config.py` (modified) — `inner_voice` + `inner_voice_path` fields (`LUMI_INNER_VOICE` /
  `LUMI_INNER_VOICE_FILE`), `DEFAULT_INNER_VOICE_PATH`.
- `core/agent.py` (modified) — `Core` takes `reasoning_directive` (default `REASONING_DIRECTIVE`, used in
  the system prompt); `build_core` loads the inner voice when on, degrading to the generic directive on a
  missing/empty file (logged via a new `lumi.core` logger).
- `.env.example` (modified) — documented the two vars.
- `tests/unit/test_inner_voice.py` (added) — 9 cases.

**Validation:**
- [x] Unit + contract tests: pass (9 new; suite green)
- [x] Lint (ruff) + py_compile: pass
- [x] Acceptance: inner voice replaces `REASONING_DIRECTIVE` when on; **off → byte-identical**; one model
      call; the file keeps the `<think>` wrap + never-competence invariant; no contract change; no paid calls.

---

### LUMI-150: LUMI_THINK_SHOW — think-show modes + the think-log tier

**Status:** completed
**Commit:** ae27129
**Files changed:**
- `core/config.py` (modified) — `think_show` field (`LUMI_THINK_SHOW` — debug/open/off; invalid → debug).
- `core/agent.py` (modified) — `Core` takes/validates `think_show` + a `think_show` property; a `lumi.think`
  logger writes the monologue when not off (the logged tier); `build_core` wires `cfg.think_show`.
- `tui/app.py` (modified) — `_render_thinking` hides the box when `think_show == "off"` (all call sites).
- `.env.example` (modified) — documented `LUMI_THINK_SHOW`.
- `tests/unit/test_think_show.py` (added, 6) + `tests/integration/test_tui.py` (an off-hides-the-box case).

**Validation:**
- [x] Unit + integration tests: pass (7 new; suite green)
- [x] Lint (ruff) + py_compile: pass
- [x] Acceptance: `off` hides the box, `debug`/`open` show it; the monologue is logged but **not persisted**
      to long-term memory (memory test pins it); no contract change; no paid calls.

## Validation summary

- **Full test suite:** `uv run pytest` → **1226 passed** (no paid APIs — the model is mocked throughout).
- **Lint:** `ruff check` clean on every changed path.
- **Contract:** `{reply, emotion, intensity}` + memory record shapes unchanged — no contract-test edits
  required. **Off → byte-identical** (existing prompt + TUI think-box tests pass verbatim).

## Next Steps

- **Release:** version not bumped (per policy). When confirmed, roadmap phase `v0.38` → semver **0.38.0**
  via `/release-version 0.38.0`.
- **Author her reasoning:** with `LUMI_INNER_VOICE=on`, edit `core/inner_voice.md` to tune *how she thinks*
  (restart to apply). `LUMI_THINK_SHOW=debug` shows the monologue; `open` surfaces it in character; `off`
  hides it.
- **Deferred (future versions):** the adaptive dynamics — voice volume by needs / social-battery /
  self-regard (v1.1–v1.2+), Автоверс anchors, and the `maturity` development axis — land on the same
  `core/inner_voice.md` seams when those states/subsystems exist (see INNER_VOICE.md §Deferred seams).
