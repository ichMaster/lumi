# Version v1.4 — Execution Report

**Date:** 2026-07-15
**Branch:** v1.1-dev
**Label:** v1::version:1
**Phase:** v1.4 — Streaming replies (TUI) + sentence-chunked voicer
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All four issues implemented, validated (mock model / mock TTS — no paid calls), committed one-per-issue,
pushed, and closed. Full suite **1529 passed**, ruff clean.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | LUMI-188 | LLMClient streaming seam | v1.4 | completed | 59239a0 | 8 | pass |
| 2 | LUMI-189 | TUI incremental render | v1.4 | completed | 06b91fa | 2 | pass |
| 3 | LUMI-190 | Sentence-chunked voicer (Phase B) | v1.4 | completed | 1c0c0f4 | 5 | pass |
| 4 | LUMI-191 | Off-pin, integration, docs | v1.4 | completed | 1adcfe0 | 3 | pass |

## Detailed Results

### LUMI-188: LLMClient streaming seam

**Status:** completed · **Commit:** 59239a0
**Files changed:**
- `core/streaming.py` (added) — `decode_json_string_value` (growing `reply` out of partial JSON) + `StreamTagFilter` (route `<think>`, hide inline tags, never leak a half-tag).
- `core/llm.py` — `reply_structured_stream` on the Protocol + every client; real streaming on Anthropic (`input_json_delta`) + Gemini (`streamGenerateContent` SSE, injectable transport); tool-turn/error → blocking fallback; other providers emit once.
- `core/agent.py` — `Core.reply(on_delta, on_think_delta)`, `_reply_streamed`, `stream_enabled`.
- `core/config.py` — `LUMI_STREAM` (off by default).
- Tests: `test_streaming.py`, `test_streaming_seam.py`, `test_streaming_turn.py`, `test_config.py`.

**Validation:** unit + integration + contract, all mocked. **Acceptance:** all criteria pass (assembled == blocking, `<think>` filter never leaks a half-tag, tool turn streams only its terminal round, malformed stream → blocking fallback, off → byte-identical).

---

### LUMI-189: TUI incremental render

**Status:** completed · **Commit:** 06b91fa
**Files changed:**
- `tui/app.py` — `#live` Static (hidden by default); `_reply_streamed` runs `core.reply(on_delta, on_think_delta)` off the UI thread, deltas marshalled via `call_from_thread` (`_grow_stream_reply` / `_grow_stream_think`); `#live` clears at completion so the final render commits to `#history` (emotion/face once).
- Tests: `tests/integration/test_tui.py` (5 new).

**Validation:** Textual pilot tests, mock model. **Acceptance:** deltas on the non-blocking worker, face once at completion, reasoning → think box, off → one-shot render.

---

### LUMI-190: Sentence-chunked voicer (Phase B)

**Status:** completed · **Commit:** 1c0c0f4
**Files changed:**
- `voice/sentences.py` (added) — `split_sentences` (ordered whole sentences, never mid-word, flush tail).
- `voice/voicer.py` — `voice_pending(..., sentences=False)`: on → synth+play per sentence; off → byte-identical.
- `core/config.py` — `LUMI_VOICE_SENTENCES` (off by default).
- Tests: `test_sentences.py` (8), `test_voicer.py` (3 new).

**Validation:** mock TTS. **Acceptance:** chunker splits/flushes correctly, voicer speaks per sentence, outbox record unchanged (Telegram byte-identical), off → whole reply as today.

---

### LUMI-191: Off-pin, integration, docs

**Status:** completed · **Commit:** 1adcfe0
**Files changed:**
- `tests/integration/test_streaming_e2e.py` (added) — full path (TUI → one outbox record → sentence voicer) + streamed-vs-blocking outbox byte-identity.
- `.env.example` — `LUMI_STREAM` + `LUMI_VOICE_SENTENCES` documented.
- `specification/features/LATENCY.md` — S3 marked shipped as v1.4.

**Validation:** integration, mock model + TTS. **Acceptance:** end-to-end streamed turn, off → byte-identical outbox/records, Telegram complete-record path, docs.

## Invariants held

- **Emotion contract unchanged** — the prose streams; `{reply, emotion, intensity}` is resolved and validated on the completed result (v0.3 gate).
- **Core interface-independent** — the streaming seam lives in `core.llm`; the TUI/voicer consume it.
- **Telegram byte-identical** — one complete outbox record regardless of streaming.
- **Off (`LUMI_STREAM` / `LUMI_VOICE_SENTENCES`) → byte-identical to v1.3.**
- **No paid calls** — all streams (Anthropic/Gemini fakes) + TTS mocked.

## Next Steps

- v1.4 is code-complete and unmerged into a release. When ready: `/release-version 1.4.0`.
- Real-provider streaming is wired (Anthropic `input_json_delta`, Gemini SSE) but exercised only via fakes in CI — a manual paid smoke-test on the live Gemini engine is recommended before relying on it in production.
- Next phase: **v1.5 — Fast turns: async POST + incremental store** (the durable latency fix, S1+S2).
