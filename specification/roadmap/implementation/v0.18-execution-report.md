# Version v0.18 — Execution Report

**Date:** 2026-06-14
**Branch:** v0.12-dev
**Label:** v0::version:0
**Phase:** v0.18 — More models (model & provider switching)
**Target version:** 0.18.0 (not yet released — awaiting confirmation)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 5 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All five issues implemented, validated (ruff + the full pytest suite, **651 passed, no paid API
calls**), committed one-per-issue, pushed, and closed on GitHub.

## Issues

| # | LUMI ID | Title | Phase | Status | Commit | Tests |
|---|---------|-------|-------|--------|--------|-------|
| 1 | LUMI-075 | build_llm factory — provider/model selection from config (+ Anthropic tiers) | v0.18 | completed | ae2dada | pass |
| 2 | LUMI-076 | OpenAI-compatible backend — OpenAI / DeepSeek / local server | v0.18 | completed | 38ab849 | pass |
| 3 | LUMI-077 | MiniMax backend | v0.18 | completed | 29fe94f | pass |
| 4 | LUMI-078 | Structured-output parity → the v0.3 gate (+ thinking/effort degradation) | v0.18 | completed | a87e57e | pass |
| 5 | LUMI-079 | Provider contract test + cross-provider parity + config-selection tests | v0.18 | completed | b43c2ca | pass |

## Detailed Results

### LUMI-075: build_llm factory — provider/model selection from config

**Status:** completed · **Commit:** ae2dada
**Files changed:** `core/llm.py`, `core/agent.py`, `core/config.py`, `tests/unit/test_llm_factory.py` (added), `.env.example`

- `build_llm(cfg) -> LLMClient` dispatches on `cfg.provider`; `build_core` uses it. `Config` typed via `TYPE_CHECKING` (no runtime coupling).
- Per-provider keys (`openai`/`deepseek`/`minimax`, secrets `repr=False`) + `llm_base_url`; only the active provider's key is required. Anthropic tiers switch by model id.
- **Validation:** ruff pass · 6 new tests · full suite 616.

### LUMI-076: OpenAI-compatible backend (OpenAI / DeepSeek / local)

**Status:** completed · **Commit:** 38ab849
**Files changed:** `core/llm.py`, `pyproject.toml`, `.env.example`, `tests/unit/test_llm_openai.py` (added), `tests/unit/test_llm_factory.py`

- `OpenAICompatibleClient` (one adapter for OpenAI/DeepSeek/local); JSON-mode structured output + shared `parse_emotion_json` that degrades malformed output to the v0.3 gate. `openai` SDK is a lazy optional extra (`models`).
- **Validation:** ruff pass · 10 new tests · full suite 626.

### LUMI-077: MiniMax backend

**Status:** completed · **Commit:** 29fe94f
**Files changed:** `core/llm.py`, `tests/unit/test_llm_minimax.py` (added), `tests/unit/test_llm_factory.py`

- `MiniMaxClient` via stdlib HTTP (no SDK dep) to `chatcompletion_v2`; non-zero `base_resp` → `LLMError`; injectable `_transport` for tests.
- **Validation:** ruff pass · 8 new tests · full suite 634.

### LUMI-078: Structured-output parity → the v0.3 gate (+ feature degradation)

**Status:** completed · **Commit:** a87e57e
**Files changed:** `core/llm.py`, `tests/unit/test_llm_parity.py` (added)

- The shared gate was already uniform (the Core runs every backend's raw dict through `validate()`); added a one-time debug log when `thinking`/`effort` are set on a non-Anthropic provider (ignored, not errors). Missing `relation` degrades to a neutral read; stats parity across backends.
- **Validation:** ruff pass · 6 new tests · full suite 640.

### LUMI-079: Provider contract test + cross-provider parity

**Status:** completed · **Commit:** b43c2ca
**Files changed:** `tests/contract/test_llm_provider_contract.py` (added)

- Parametrised LLMClient + emotion-field contract over all four backends; full `Core.reply` turn per provider yields a valid `EmotionState` + persists both messages; a provider error surfaces as `LLMError`. Zero paid calls.
- **Validation:** ruff pass · 11 new tests · full suite **651**.

## Contracts honoured

- The locked emotion field `{reply, emotion, intensity}` is **unchanged** — only a new producer sits behind the v0.3 gate per provider.
- The core stays **interface-independent**: it depends only on the `LLMClient` seam; no SDK on `core`'s import path (Anthropic + OpenAI lazy-imported; MiniMax is stdlib HTTP).
- **No paid APIs in CI** — every backend is exercised through a stubbed transport / mock SDK.

## Next Steps

- v0.18 is feature-complete. Release when ready: `/release-version 0.18.0` (the phase → semver `0.18.0`).
- No remaining issues under `v0::version:0`.
